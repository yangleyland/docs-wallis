---
title: 'Desarrollo'
description: 'Aprende a previsualizar los cambios de forma local'
og:title: "Development | Firecrawl"
og:description: "Aprende a previsualizar los cambios de forma local"
---

<Info>
  **Requisito previo** Debes tener instalado Node.js (versión 18.10.0 o
  superior).
</Info>

Paso 1. Instala Mintlify en tu sistema operativo:

<CodeGroup>
  ```bash npm
  npm i -g mintlify
  ```

  ```bash yarn
  yarn global add mintlify
  ```
</CodeGroup>

Paso 2. Ve al directorio donde están las docs (donde puedes encontrar `mint.json`) y ejecuta el siguiente comando:

```bash
mintlify dev
```

El sitio web de la documentación ya está disponible en `http://localhost:3000`.

<div id="custom-ports">
  ### Puertos personalizados
</div>

Mintlify usa el puerto 3000 de forma predeterminada. Puedes usar la opción `--port` para personalizar el puerto en el que se ejecuta Mintlify. Por ejemplo, usa este comando para ejecutarlo en el puerto 3333:

```bash
mintlify dev --port 3333
```

Verás un error como este si intentas ejecutar Mintlify en un puerto que ya está ocupado:

```md
Error: listen EADDRINUSE: la dirección ya está en uso :::3000
```

<div id="mintlify-versions">
  ## Versiones de Mintlify
</div>

Cada CLI está vinculado a una versión específica de Mintlify. Actualiza la CLI si tu sitio web local se ve diferente al de producción.

<CodeGroup>
  ```bash npm
  npm i -g mintlify@latest
  ```

  ```bash yarn
  yarn global upgrade mintlify
  ```
</CodeGroup>

<div id="deployment">
  ## Despliegue
</div>

<Tip>
  Editores ilimitados disponibles con el [Plan Startup](https://mintlify.com/pricing)
</Tip>

Deberías ver lo siguiente si el despliegue se realizó correctamente:

<Frame>
  <img src="/images/checks-passed.png" style={{ borderRadius: '0.5rem' }} />
</Frame>

<div id="troubleshooting">
  ## Solución de problemas
</div>

Aquí verás cómo resolver algunos problemas comunes al trabajar con la CLI.

<AccordionGroup>
  <Accordion title="Mintlify no se carga">
    Actualiza a Node v18. Ejecuta `mintlify install` e inténtalo de nuevo.
  </Accordion>

  <Accordion title="Error «No such file or directory» en Windows">
    Ve al directorio `C:/Users/Username/.mintlify/` y elimina la carpeta `mint`.
    Luego abre Git Bash en esta ubicación y ejecuta `git clone
    https://github.com/mintlify/mint.git`.

    Repite el paso 3.
  </Accordion>

  <Accordion title="Aparece un error desconocido">
    Ve a la raíz de tu sistema y elimina la carpeta ~/.mintlify.
    Luego ejecuta `mintlify dev` de nuevo.
  </Accordion>
</AccordionGroup>

¿Te interesa qué cambió en una versión de la CLI? [Consulta el registro de cambios de la CLI.](/es/changelog/command-line)