---
title: 'Crawl'
description: 'Firecrawl puede explorar recursivamente los subdominios de una URL y recopilar su contenido'
icon: 'spider'
og:title: "Crawl | Firecrawl"
og:description: "Firecrawl puede explorar recursivamente los subdominios de una URL y recopilar su contenido"
---

Firecrawl rastrea sitios web a fondo, garantizando una extracción de datos completa mientras gestiona infraestructuras web complejas. Así es como funciona:

1. **Análisis de URL:**
   Empieza con una URL específica, identifica enlaces revisando el sitemap y luego rastrea el sitio. Si no se encuentra un sitemap, rastrea el sitio siguiendo los enlaces.

2. **Recorrido recursivo:**
   Sigue cada enlace de forma recursiva para descubrir todas las subpáginas.

3. **Extracción de contenido:**
   Recopila contenido de cada página visitada y maneja complejidades como el renderizado de JavaScript o los límites de tasa.

4. **Compilación de resultados:**
   Convierte los datos recopilados en markdown limpio o en una salida estructurada, ideal para el procesamiento por LLM u otras tareas.

Este método garantiza un rastreo y una recopilación de datos exhaustivos desde cualquier URL de inicio.

<div id="crawling">
  ## Rastreado
</div>

<div id="crawl-endpoint">
  ### punto de conexión /crawl
</div>

Se usa para rastrear una URL y todas las subpáginas accesibles. Envía un trabajo de rastreo y devuelve un ID de trabajo para consultar el estado del rastreo.

<div id="installation">
  ### Instalación
</div>

<CodeGroup>
  ```bash Python
  pip install firecrawl-py
  ```

  ```bash JavaScript
  npm install firecrawl
  ```

  ```go Go
  go get github.com/mendableai/firecrawl-go
  ```

  ```toml Rust
  # agrega lo siguiente a tu Cargo.toml

  [dependencies]
  firecrawl = "^0.1"
  tokio = { version = "^1", features = ["full"] }
  serde = { version = "^1.0", features = ["derive"] }
  serde_json = "^1.0"
  uuid = { version = "^1.10", features = ["v4"] }

  [build-dependencies]
  tokio = { version = "1", features = ["full"] }
  ```
</CodeGroup>

<div id="usage">
  ### Uso
</div>

<CodeGroup>
  ```python Python
  from firecrawl import Firecrawl

  app = Firecrawl(api_key="YOUR_API_KEY")

  crawl_result = app.crawl_url('mendable.ai', {'crawlerOptions': {'excludes': ['blog/*']}})

  # Obtener el markdown
  for result in crawl_result:
      print(result['markdown'])
  ```

  ```js JavaScript
  import { Firecrawl } from 'firecrawl';

  // Inicializa Firecrawl con tu clave de API
  const app = new Firecrawl({ apiKey: 'YOUR_API_KEY' });

  // Hace Crawl de un sitio web
  const crawlResult = await app.crawlUrl('mendable.ai', { crawlerOptions: { excludes: ['blog/*'] } });

  // Muestra el markdown en la consola
  console.log(crawlResult.map(result => result.markdown));
  ```

  ```go Go
  import (
    "fmt"
    "log"

    "github.com/mendableai/firecrawl-go"
  )

  func main() {
    app, err := firecrawl.NewFirecrawlApp("YOUR_API_KEY")
    if err != nil {
      log.Fatalf("No se pudo inicializar Firecrawl: %v", err)
    }

    crawlResult, err := app.CrawlURL("mendable.ai", nil)
    if err != nil {
      log.Fatalf("No se pudo hacer Crawl de la URL: %v", err)
    }

    fmt.Println(crawlResult)
  }
  ```

  ```rust Rust
  use firecrawl::Firecrawl;

  #[tokio::main]
  async fn main() {
    // Inicializa Firecrawl con la clave de API
    let api_key = "YOUR_API_KEY";
    let api_url = "https://api.firecrawl.dev";
    let app = Firecrawl::new(api_key, api_url).expect("No se pudo inicializar Firecrawl");

    // Hace Crawl de la URL
    let crawl_params = json!({
      "crawlerOptions": {
          "excludes": ["blog/*"]
      }
    });

    let crawl_result = app
        .crawl_url("https://example.com", Some(crawl_params), true, 2, None)
        .await;

    // Imprime el resultado del Crawl
    match crawl_result {
        Ok(data) => println!("Resultado del Crawl:\n{}", data),
        Err(e) => eprintln!("El Crawl falló: {}", e),
    }
  }
  ```

  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v0/crawl \
      -H 'Content-Type: application/json' \
      -H 'Authorization: Bearer YOUR_API_KEY' \
      -d '{
        "url": "https://mendable.ai"
      }'
  ```
</CodeGroup>

<div id="job-id-response">
  ### Respuesta de ID de trabajo
</div>

Si no usas el SDK o prefieres usar un webhook u otro método de sondeo, puedes establecer `wait_until_done` en `false`.
Esto devolverá un jobId.

Con cURL, /crawl siempre devolverá un jobId que puedes usar para verificar el estado del rastreo.

```json
{ "jobId": "1234-5678-9101" }
```

<div id="check-crawl-job">
  ### Comprobar trabajo de rastreo
</div>

Se usa para comprobar el estado de un trabajo de rastreo y obtener su resultado.

<CodeGroup>

```python Python
status = app.check_crawl_status(job_id)
```

```js JavaScript
const status = await app.checkCrawlStatus(jobId);
```

```go Go
status, err := app.CheckCrawlStatus(jobId)
if err != nil {
  log.Fatalf("Failed to check crawl status: %v", err)
}
```

```rust Rust
let status = match app.check_crawl_status(jobId).await {
    Ok(status) => status,
    Err(e) => panic!("Failed to check crawl status: {:?}", e),
};

println!("Crawl Status: {:?}", status);
```

```bash cURL
curl -X GET https://api.firecrawl.dev/v0/crawl/status/1234-5678-9101 \
  -H 'Content-Type: application/json' \
  -H 'Authorization: Bearer YOUR_API_KEY'
```

</CodeGroup>

<div id="response">
  #### Respuesta
</div>

```json
{
  "status": "completado",
  "current": 22,
  "total": 22,
  "data": [
    {
      "content": "Contenido sin procesar ",
      "markdown": "# Contenido en Markdown",
      "provider": "web-scraper",
      "metadata": {
        "title": "Mendable | IA para CX y Ventas",
        "description": "IA para CX y Ventas"
        "language": null,
        "sourceURL": "https://www.mendable.ai/"
      }
    }
  ]
}
```
