---
title: 'Go'
description: 'El SDK de Go de Firecrawl es un wrapper de la API de Firecrawl que te ayuda a convertir sitios web en Markdown fácilmente.'
icon: 'golang'
og:title: "SDK de Go | Firecrawl"
og:description: "El SDK de Go de Firecrawl es un wrapper de la API de Firecrawl que te ayuda a convertir sitios web en Markdown fácilmente."
---

import InstallGoSDK from "/snippets/es/v1/installation/go.mdx";
import ScrapeAndCrawlGoExample from "/snippets/es/v1/scrape-and-crawl/go.mdx";
import ScrapeGoShort from "/snippets/es/v1/scrape/short/go.mdx";
import CrawlGoShort from "/snippets/es/v1/crawl/short/go.mdx";
import CheckCrawlStatusGoShort from "/snippets/es/v1/crawl-status/short/go.mdx";
import MapGoShort from "/snippets/es/v1/map/short/go.mdx";

<Warning>
  Este SDK utiliza actualmente la versión **v1** de la API de Firecrawl, que no es la más reciente (la v2 está disponible). Algunas funciones y mejoras podrían estar disponibles únicamente en v2.
</Warning>

<div id="installation">
  ## Instalación
</div>

Para instalar el SDK de Firecrawl para Go, puedes usar go get:

<InstallGoSDK />

<div id="usage">
  ## Uso
</div>

1. Obtén una clave de API en [firecrawl.dev](https://firecrawl.dev)
2. Establece la `API key` como parámetro en la estructura `FirecrawlApp`.
3. Establece la `API URL` y/o pásala como parámetro a la estructura `FirecrawlApp`. De forma predeterminada es `https://api.firecrawl.dev`.
4. Establece la `version` y/o pásala como parámetro a la estructura `FirecrawlApp`. De forma predeterminada es `v1`.

Aquí tienes un ejemplo de cómo usar el SDK con manejo de errores:

<ScrapeAndCrawlGoExample />

<div id="scraping-a-url">
  ### Extraer una URL
</div>

Para extraer una única URL con manejo de errores, usa el método `ScrapeURL`. Recibe la URL como parámetro y devuelve los datos extraídos como un diccionario.

<ScrapeGoShort />

<div id="crawling-a-website">
  ### Rastreo de un sitio web
</div>

Para rastrear un sitio web, usa el método `CrawlUrl`. Recibe la URL inicial y parámetros opcionales como argumentos. El argumento `params` te permite especificar opciones adicionales para el trabajo de rastreo, como el número máximo de páginas a rastrear, los dominios permitidos y el formato de salida.

<CrawlGoShort />

<div id="checking-crawl-status">
  ### Comprobar el estado del rastreo
</div>

Para verificar el estado de un trabajo de rastreo, usa el método `CheckCrawlStatus`. Recibe el ID del trabajo como parámetro y devuelve el estado actual del rastreo.

<CheckCrawlStatusGoShort />

<div id="map-a-website">
  ### Mapear un sitio web
</div>

Usa `MapUrl` para generar una lista de URL de un sitio web. El argumento `params` te permite personalizar el proceso de mapeo, incluidas opciones para excluir subdominios o usar el sitemap.
 
<MapGoShort />

<div id="error-handling">
  ## Manejo de errores
</div>

El SDK gestiona los errores devueltos por la API de Firecrawl y arroja las excepciones correspondientes. Si se produce un error durante una solicitud, se lanzará una excepción con un mensaje descriptivo.