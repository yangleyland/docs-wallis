---
title: Trechos reutilizáveis
description: Trechos personalizados reutilizáveis para manter o conteúdo em sincronia
icon: 'recycle'
---

import SnippetIntro from '/snippets/pt-BR/snippet-intro.mdx';

<SnippetIntro />

<div id="creating-a-custom-snippet">
  ## Criando um snippet personalizado
</div>

**Pré-requisito**: Você deve criar o arquivo do snippet no diretório `snippets`.

<Note>
  Qualquer página no diretório `snippets` será tratada como um snippet e não
  será renderizada como uma página independente. Se você quiser criar uma página
  independente a partir do snippet, importe-o em outro arquivo e use-o como um
  componente.
</Note>

<div id="default-export">
  ### Exportação padrão
</div>

1. Adicione conteúdo ao arquivo de snippet que você quer reutilizar em vários
   locais. Opcionalmente, você pode adicionar variáveis que podem ser preenchidas via props
   ao importar o snippet.

```mdx snippets/meu-snippet.mdx
Olá, mundo! Este é o conteúdo que quero reutilizar em várias páginas. Minha palavra-chave do
dia é {word}.
```

<Warning>
  O conteúdo que você deseja reutilizar deve estar dentro do diretório `snippets`
  para que a importação funcione.
</Warning>

2. Importe o snippet no arquivo de destino.

```mdx arquivo-de-destino.mdx
---
title: Meu título
description: Minha descrição
---

import MySnippet from '/snippets/path/to/my-snippet.mdx';

## Cabeçalho

Lorem impsum dolor sit amet.

<MySnippet word="bananas" />
```

<div id="reusable-variables">
  ### Variáveis reutilizáveis
</div>

1. Exporte uma variável do seu arquivo de snippet:

```mdx trechos/caminho/para/variaveis-personalizadas.mdx
export const myName = 'meu nome';

export const myObject = { fruit: 'morangos' };
```

2. Importe o trecho do seu arquivo de destino e use a variável:

```mdx arquivo-de-destino.mdx
---
title: Meu título
description: Minha descrição
---

import { myName, myObject } from '/snippets/path/to/custom-variables.mdx';

Olá, meu nome é {myName} e eu gosto de {myObject.fruit}.
```

<div id="reusable-components">
  ### Componentes reutilizáveis
</div>

1. No seu arquivo de snippet, crie um componente que receba props exportando-o
   como uma função de seta (arrow function).

```mdx snippets/custom-component.mdx
export const MyComponent = ({ title }) => (
  <div>
    <h1>{title}</h1>
    <p>... trecho do snippet ...</p>
  </div>
);
```

<Warning>
  MDX não compila dentro do corpo de uma função arrow. Prefira a sintaxe HTML
  quando possível ou use uma exportação padrão se precisar usar MDX.
</Warning>

2. Importe o snippet no arquivo de destino e passe as props

```mdx arquivo-de-destino.mdx
---
title: Meu título
description: Minha descrição
---

import { MyComponent } from '/snippets/custom-component.mdx';

Lorem ipsum dolor sit amet.

<MyComponent title={'Título personalizado'} />
```
