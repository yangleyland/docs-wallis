---
title: 'Rust'
description: 'O Firecrawl Rust SDK é uma biblioteca que ajuda você a extrair e rastrear sites com facilidade, gerando dados em um formato pronto para uso com modelos de linguagem (LLMs).'
icon: 'rust'
og:title: "Rust SDK | Firecrawl"
og:description: "O Firecrawl Rust SDK é uma biblioteca que ajuda você a extrair e rastrear sites com facilidade, gerando dados em um formato pronto para uso com modelos de linguagem (LLMs)."
---

import InstallationRust from '/snippets/pt-BR/v1/installation/rust.mdx'
import ScrapeAndCrawlRustExample from '/snippets/pt-BR/v1/scrape-and-crawl/rust.mdx'
import ScrapeRustShort from '/snippets/pt-BR/v1/scrape/short/rust.mdx'
import CrawlRustShort from '/snippets/pt-BR/v1/crawl/short/rust.mdx'
import CrawlAsyncRustShort from '/snippets/pt-BR/v1/crawl-async/short/rust.mdx'
import MapRustShort from '/snippets/pt-BR/v1/map/short/rust.mdx'
import LLMExtractRust from '/snippets/pt-BR/v1/llm-extract/base/rust.mdx'

<Warning>
  Este SDK atualmente usa a versão **v1** da API do Firecrawl, que não é a mais recente (a v2 está disponível). Alguns recursos e melhorias podem estar disponíveis apenas na v2.
</Warning>

<div id="installation">
  ## Instalação
</div>

Para instalar o SDK do Firecrawl para Rust, adicione o seguinte ao seu `Cargo.toml`:

<InstallationRust />

<div id="usage">
  ## Uso
</div>

Primeiro, obtenha uma chave de API em [firecrawl.dev](https://firecrawl.dev). Depois, inicialize o `FirecrawlApp`. A partir daí, você pode acessar funções como `FirecrawlApp::scrape_url`, que permitem usar nossa API.

Veja um exemplo de uso do SDK em Rust:

<ScrapeAndCrawlRustExample />

<div id="scraping-a-url">
  ### Extraindo dados de uma URL
</div>

Para extrair dados de uma única URL, use o método `scrape_url`. Ele recebe a URL como parâmetro e retorna os dados coletados como um `Document`.

<ScrapeRustShort />

<div id="scraping-with-extract">
  ### Extração com o Extract
</div>

Com o Extract, você pode extrair facilmente dados estruturados de qualquer URL. É necessário especificar o seu esquema no formato JSON Schema, usando a macro `serde_json::json!`.

<LLMExtractRust />

<div id="crawling-a-website">
  ### Rastrear um site
</div>

Para rastrear um site, use o método `crawl_url`. Ele aguardará a conclusão do rastreamento, o que pode levar bastante tempo dependendo da URL inicial e das opções escolhidas.

<CrawlRustShort />

<div id="crawling-asynchronously">
  #### Rastreamento assíncrono
</div>

Para rastrear sem aguardar o resultado, use o método `crawl_url_async`. Ele aceita os mesmos parâmetros, mas retorna uma struct `CrawlAsyncRespone` contendo o ID do rastreamento. Você pode usar esse ID com o método `check_crawl_status` para verificar o status a qualquer momento. Observe que rastreamentos concluídos são excluídos após 24 horas.

<CrawlAsyncRustShort />

<div id="map-a-url">
  ### Mapear uma URL
</div>

Mapeie todos os links associados a partir de uma URL de origem.

<MapRustShort />

<div id="error-handling">
  ## Tratamento de erros
</div>

O SDK trata os erros retornados pela API do Firecrawl e por nossas dependências, e os unifica no enum `FirecrawlError`, que implementa `Error`, `Debug` e `Display`. Todos os nossos métodos retornam um `Result<T, FirecrawlError>`.