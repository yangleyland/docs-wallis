---
title: "Raspagem mais rápida"
description: "Acelere suas raspagens em 500% com o parâmetro maxAge"
og:title: "Raspagem mais rápida | Firecrawl"
og:description: "Acelere suas raspagens em 500% com o parâmetro maxAge"
---

import FastScrapePython from "/snippets/pt-BR/v1/scrape/fast/python.mdx";
import FastScrapeNode from "/snippets/pt-BR/v1/scrape/fast/js.mdx";
import FastScrapeGo from "/snippets/pt-BR/v1/scrape/fast/go.mdx";
import FastScrapeRust from "/snippets/pt-BR/v1/scrape/fast/rust.mdx";
import FastScrapeCURL from "/snippets/pt-BR/v1/scrape/fast/curl.mdx";
import FastCrawlPython from "/snippets/pt-BR/v1/crawl/fast/python.mdx";
import FastCrawlNode from "/snippets/pt-BR/v1/crawl/fast/js.mdx";
import FastCrawlGo from "/snippets/pt-BR/v1/crawl/fast/go.mdx";
import FastCrawlRust from "/snippets/pt-BR/v1/crawl/fast/rust.mdx";
import FastCrawlCURL from "/snippets/pt-BR/v1/crawl/fast/curl.mdx";

<div id="how-it-works">
  ## Como Funciona
</div>

Obtenha resultados de scraping **500% mais rápido** quando você não precisar dos dados mais atualizados. O Firecrawl mantém um índice de páginas já raspadas. Adicione o parâmetro `maxAge` à sua solicitação e nós vamos:

1. **Retornar na hora** se tivermos uma versão recente da página
2. **Raspar novamente** apenas se nossa versão for mais antiga que a idade especificada
3. **Economizar seu tempo** — os resultados chegam em milissegundos, não em segundos

<div id="when-to-use-this">
  ## Quando usar
</div>

**Ideal para:**

- Documentação, artigos, páginas de produto
- Processamento em lote
- Desenvolvimento e testes
- Criação de bases de conhecimento

**Evite para:**

- Dados em tempo real (cotações, placares ao vivo, notícias de última hora)
- Conteúdo atualizado com frequência
- Aplicações sensíveis ao tempo

<div id="usage">
  ## Uso
</div>

Basta adicionar o parâmetro `maxAge` à sua requisição de scrape. O valor está em milissegundos — por exemplo, `3600000` significa “usar os dados em cache se tiverem menos de 1 hora”.

<CodeGroup>

<FastScrapePython />

<FastScrapeNode />

<FastScrapeGo />

<FastScrapeRust />

<FastScrapeCURL />

</CodeGroup>

<div id="common-maxage-values">
  ## Valores comuns de maxAge
</div>

Aqui estão alguns valores de referência úteis:

- **5 minutos**: `300000` - Para conteúdo semidinâmico
- **1 hora**: `3600000` - Para conteúdo atualizado de hora em hora
- **1 dia**: `86400000` - Para conteúdo atualizado diariamente
- **1 semana**: `604800000` - Para conteúdo relativamente estático

<div id="performance-impact">
  ## Impacto no Desempenho
</div>

Com `maxAge` ativado:

- **Tempos de resposta 500% mais rápidos** para conteúdo recente
- **Resultados instantâneos** em vez de aguardar novas coletas

<div id="important-notes">
  ## Notas importantes
</div>

- **Padrão**: `maxAge` é `0` por padrão, o que significa sempre coletar conteúdo novo
- **Atual quando necessário**: Se nossos dados forem mais antigos que `maxAge`, coletamos novamente automaticamente
- **Sem dados desatualizados**: Você nunca receberá dados mais antigos que o `maxAge` especificado

<div id="faster-crawling">
  ## Rastreamento mais rápido
</div>

Os mesmos ganhos de velocidade se aplicam ao rastrear várias páginas. Use `maxAge` em `scrapeOptions` para obter resultados em cache de páginas que acessamos recentemente.

<CodeGroup>

<FastCrawlPython />

<FastCrawlNode />

<FastCrawlGo />

<FastCrawlRust />

<FastCrawlCURL />

</CodeGroup>

Ao rastrear com `maxAge`, cada página do seu rastreamento terá um ganho de velocidade de 500% se houver dados recentes em cache para ela.

Comece a usar `maxAge` hoje para coletas e rastreamentos muito mais rápidos!