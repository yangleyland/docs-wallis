---
title: "Pesquisa"
description: "Pesquise na web e obtenha o conteúdo completo dos resultados"
og:title: "Pesquisa | Firecrawl"
og:description: "Pesquise na web e obtenha o conteúdo completo da página a partir dos resultados"
icon: "magnifying-glass"
---

import InstallationPython from "/snippets/pt-BR/v1/installation/python.mdx";
import InstallationNode from "/snippets/pt-BR/v1/installation/js.mdx";
import InstallationGo from "/snippets/pt-BR/v1/installation/go.mdx";
import InstallationRust from "/snippets/pt-BR/v1/installation/rust.mdx";
import SearchPython from "/snippets/pt-BR/v1/search/base/python.mdx";
import SearchNode from "/snippets/pt-BR/v1/search/base/js.mdx";
import SearchCURL from "/snippets/pt-BR/v1/search/base/curl.mdx";
import SearchContentPython from "/snippets/pt-BR/v1/search/content/python.mdx";
import SearchContentNode from "/snippets/pt-BR/v1/search/content/js.mdx";
import SearchContentCURL from "/snippets/pt-BR/v1/search/content/curl.mdx";
import SearchLocationPython from "/snippets/pt-BR/v1/search/location/python.mdx";
import SearchLocationNode from "/snippets/pt-BR/v1/search/location/js.mdx";
import SearchLocationCURL from "/snippets/pt-BR/v1/search/location/curl.mdx";
import SearchTimePython from "/snippets/pt-BR/v1/search/time/python.mdx";
import SearchTimeNode from "/snippets/pt-BR/v1/search/time/js.mdx";
import SearchTimeCURL from "/snippets/pt-BR/v1/search/time/curl.mdx";

A API de pesquisa do Firecrawl permite realizar buscas na web e, opcionalmente, fazer scraping dos resultados em uma única operação.

* Escolha formatos de saída específicos (markdown, HTML, links, screenshots)
* Pesquise na web com parâmetros personalizáveis (localização, etc.)
* Opcionalmente recupere o conteúdo dos resultados de pesquisa em vários formatos
* Controle a quantidade de resultados e defina tempos limite

Para mais detalhes, consulte a [Referência da API do endpoint /search](https://docs.firecrawl.dev/api-reference/endpoint/search).

<div id="performing-a-search-with-firecrawl">
  ## Fazendo uma busca com o Firecrawl
</div>

<div id="search-endpoint">
  ### endpoint /search
</div>

Usado para fazer pesquisas na web e, opcionalmente, obter conteúdo dos resultados.

<div id="installation">
  ### Instalação
</div>

<CodeGroup>

  <InstallationPython />

  <InstallationNode />

  <InstallationGo />
  
  <InstallationRust />

</CodeGroup>

<div id="basic-usage">
  ### Uso básico
</div>

<CodeGroup>

  <SearchPython />

  <SearchNode />
  
  <SearchCURL />

</CodeGroup>

<div id="response">
  ### Resposta
</div>

Os SDKs retornarão o objeto de dados diretamente. O cURL retornará a carga útil completa.

```json
{
  "success": true,
  "data": [
    {
      "title": "Firecrawl - A API definitiva de web scraping",
      "description": "O Firecrawl é uma poderosa API de web scraping que transforma qualquer site em dados limpos e estruturados para IA e análise.",
      "url": "https://firecrawl.dev/"
    },
    {
      "title": "Web scraping com Firecrawl - Guia completo",
      "description": "Aprenda a usar o Firecrawl para extrair dados de sites com facilidade.",
      "url": "https://firecrawl.dev/guides/web-scraping/"
    },
    {
      "title": "Documentação do Firecrawl - Introdução",
      "description": "Documentação oficial da API de web scraping do Firecrawl.",
      "url": "https://docs.firecrawl.dev/"
    }
    // ... mais resultados
  ]
}
```

<div id="search-with-content-scraping">
  ## Busca com Extração de Conteúdo
</div>

Pesquise e recupere conteúdo dos resultados de busca em uma única operação.

<CodeGroup>

  <SearchContentPython />

  <SearchContentNode />
  
  <SearchContentCURL />

</CodeGroup>

<div id="response-with-scraped-content">
  ### Resposta com conteúdo raspado
</div>

```json
{
  "success": true,
  "data": [
    {
      "title": "Firecrawl - A API definitiva de web scraping",
      "description": "A Firecrawl é uma poderosa API de web scraping que transforma qualquer site em dados limpos e estruturados para IA e análises.",
      "url": "https://firecrawl.dev/",
      "markdown": "# Firecrawl\n\nA API definitiva de web scraping\n\n## Transforme qualquer site em dados limpos e estruturados\n\nA Firecrawl facilita a extração de dados de sites para aplicações de IA, pesquisa de mercado, agregação de conteúdo e muito mais...",
      "links": [
        "https://firecrawl.dev/pricing",
        "https://firecrawl.dev/docs",
        "https://firecrawl.dev/guides",
        // ... mais links
      ],
      "metadata": {
        "title": "Firecrawl - A API definitiva de web scraping",
        "description": "A Firecrawl é uma poderosa API de web scraping que transforma qualquer site em dados limpos e estruturados para IA e análises.",
        "sourceURL": "https://firecrawl.dev/",
        "statusCode": 200
      }
    },
    // ... mais resultados
  ]
}
```

<div id="advanced-search-options">
  ## Opções avançadas de pesquisa
</div>

A API de pesquisa do Firecrawl oferece diversos parâmetros para personalizar sua pesquisa:

<div id="location-customization">
  ### Personalização de localização
</div>

<CodeGroup>

  <SearchLocationPython />

  <SearchLocationNode />
  
  <SearchLocationCURL />

</CodeGroup>

<div id="time-based-search">
  ### Pesquisa por período
</div>

Use o parâmetro `tbs` para filtrar resultados por período:

<CodeGroup>
  <SearchTimePython />

  <SearchTimeNode />

  <SearchTimeCURL />
</CodeGroup>

Valores comuns de `tbs`:

* `qdr:h` - Última hora
* `qdr:d` - Últimas 24 horas
* `qdr:w` - Última semana
* `qdr:m` - Último mês
* `qdr:y` - Último ano

Para um filtro temporal mais preciso, você pode especificar intervalos de datas exatos usando o formato de intervalo personalizado:

<CodeGroup>
  ```python Python
  from firecrawl import Firecrawl

  # Inicialize o cliente com sua chave de API
  app = Firecrawl(api_key="fc-YOUR_API_KEY")

  # Pesquisar resultados de dezembro de 2024
  search_result = app.search(
      "firecrawl updates",
      limit=10,
      tbs="cdr:1,cd_min:12/1/2024,cd_max:12/31/2024"
  )
  ```

  ```js JavaScript
  import { Firecrawl } from 'firecrawl';

  // Inicialize o cliente com sua chave de API
  const app = new Firecrawl({apiKey: "fc-YOUR_API_KEY"});

  // Pesquisar resultados de dezembro de 2024
  app.search("firecrawl updates", {
    limit: 10,
    tbs: "cdr:1,cd_min:12/1/2024,cd_max:12/31/2024"
  })
  .then(searchResult => {
    console.log(searchResult.data);
  });
  ```

  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v1/search \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer fc-YOUR_API_KEY" \
    -d '{
      "query": "firecrawl updates",
      "limit": 10,
      "tbs": "cdr:1,cd_min:12/1/2024,cd_max:12/31/2024"
    }'
  ```
</CodeGroup>

<div id="custom-timeout">
  ### Timeout personalizado
</div>

Defina um timeout personalizado para operações de busca:

<CodeGroup>
  ```python Python
  from firecrawl import Firecrawl

  # Inicialize o cliente com sua chave de API
  app = Firecrawl(api_key="fc-YOUR_API_KEY")

  # Defina um timeout de 30 segundos
  search_result = app.search(
      "complex search query",
      limit=10,
      timeout=30000  # 30 segundos em milissegundos
  )
  ```

  ```js JavaScript
  import { Firecrawl } from 'firecrawl';

  // Inicialize o cliente com sua chave de API
  const app = new Firecrawl({apiKey: "fc-YOUR_API_KEY"});

  // Defina um timeout de 30 segundos
  app.search("complex search query", {
    limit: 10,
    timeout: 30000  // 30 segundos em milissegundos
  })
  .then(searchResult => {
    // Processar resultados
    console.log(searchResult.data);
  });
  ```

  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v1/search \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer fc-YOUR_API_KEY" \
    -d '{
      "query": "complex search query",
      "limit": 10,
      "timeout": 30000
    }'
  ```
</CodeGroup>

<div id="scraping-options">
  ## Opções de scraping
</div>

Ao fazer scraping de resultados de busca, você pode especificar múltiplos formatos de saída e opções avançadas de scraping:

<CodeGroup>
  ```python Python
  from firecrawl import Firecrawl, ScrapeOptions

  # Inicialize o cliente com sua chave de API
  app = Firecrawl(api_key="fc-YOUR_API_KEY")

  # Obtenha resultados de busca com vários formatos
  search_result = app.search(
      "firecrawl features",
      limit=3,
      scrape_options=ScrapeOptions(formats=["markdown", "html", "links", "screenshot"])
  )
  ```

  ```js JavaScript
  import { Firecrawl } from 'firecrawl';

  // Inicialize o cliente com sua chave de API
  const app = new Firecrawl({apiKey: "fc-YOUR_API_KEY"});

  // Obtenha resultados de busca com vários formatos
  app.search("firecrawl features", {
    limit: 3,
    scrapeOptions: {
      formats: ["markdown", "html", "links", "screenshot"]
    }
  })
  .then(searchResult => {
    // Processe os resultados em vários formatos
    console.log(searchResult.data);
  });
  ```

  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v1/search \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer fc-YOUR_API_KEY" \
    -d '{
      "query": "firecrawl features",
      "limit": 3,
      "scrapeOptions": {
        "formats": ["markdown", "html", "links", "screenshot"]
      }
    }'
  ```
</CodeGroup>

Formatos disponíveis:

* `markdown`: Conteúdo em Markdown limpo e formatado
* `html`: Conteúdo HTML processado
* `rawHtml`: Conteúdo HTML sem modificações
* `links`: Lista de links encontrados na página
* `screenshot`: Captura de tela da página
* `screenshot@fullPage`: Captura de tela da página inteira
* `extract`: Extração de dados estruturados

<div id="cost-implications">
  ## Implicações de custo
</div>

O custo de uma busca é de 2 créditos a cada 10 resultados de busca. Se as opções de scraping estiverem ativadas, os custos padrão de scraping se aplicam a cada resultado de busca:

- **Basic scrape**: 1 crédito por página da web
- **PDF parsing**: 1 crédito por página de PDF
- **Enhanced proxy mode**: 4 créditos adicionais por página da web
- **JSON mode**: 4 créditos adicionais por página da web

Para ajudar a controlar os custos:

- Defina `parsers: []` se a análise de PDF não for necessária
- Use `proxy: "basic"` em vez de `"enhanced"` quando possível, ou defina como `"auto"`
- Limite o número de resultados de busca com o parâmetro `limit`

<div id="advanced-scraping-options">
  ## Opções avançadas de scraping
</div>

Para mais detalhes sobre as opções de scraping, consulte a [documentação do recurso de Scrape](https://docs.firecrawl.dev/features/scrape). Tudo, exceto o FIRE-1 (Agente) e o recurso de rastreioDeMudanças, é compatível com este endpoint de Search.