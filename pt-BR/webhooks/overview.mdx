---
title: "Visão geral"
description: "Notificações em tempo real para suas operações no Firecrawl"
og:title: "Webhooks | Firecrawl"
og:description: "Notificações em tempo real para suas operações no Firecrawl"
icon: "link"
---

import CrawlWebhookCURL from "/snippets/pt-BR/v2/crawl-webhook/base/curl.mdx";
import BatchScrapeWebhookCURL from "/snippets/pt-BR/v2/batch-scrape-webhook/base/curl.mdx";
import WebhookConfigBasic from "/snippets/pt-BR/v2/webhook-config/basic/json.mdx";

Receba notificações assim que um rastreamento, uma extração em lote, uma extração, um job de agente ou uma verificação de monitor começar, avançar ou terminar. Em vez de consultar o status periodicamente, você fornece um endpoint HTTPS e o Firecrawl envia eventos para ele em tempo real.

<div id="supported-operations">
  ## Operações compatíveis
</div>

| Operação         | Eventos                                                 |
| ---------------- | ------------------------------------------------------- |
| rastreamento     | `started`, `page`, `completed`                          |
| extração em lote | `started`, `page`, `completed`                          |
| extração         | `started`, `completed`, `failed`                        |
| agente           | `started`, `action`, `completed`, `failed`, `cancelled` |
| Monitor          | `check.completed`                                       |

Consulte [Tipos de evento](/pt-BR/webhooks/events) para ver os detalhes completos do payload e exemplos.

<div id="configuration">
  ## Configuração
</div>

Adicione um objeto `webhook` à sua requisição:

<WebhookConfigBasic />

| Campo      | Tipo   | Obrigatório | Descrição                                          |
| ---------- | ------ | ----------- | -------------------------------------------------- |
| `url`      | string | Sim         | URL do endpoint do seu webhook (HTTPS)            |
| `headers`  | object | Não         | Cabeçalhos personalizados para incluir nas requisições do webhook |
| `metadata` | object | Não         | Dados personalizados incluídos nas cargas do webhook |
| `events`   | array  | Não         | Tipos de evento a receber (padrão: todos)         |

<div id="usage">
  ## Uso
</div>

<div id="crawl-with-webhook">
  ### Rastreamento com webhook
</div>

<CrawlWebhookCURL />

<div id="batch-scrape-with-webhook">
  ### Raspagem em lote com webhook
</div>

<BatchScrapeWebhookCURL />

<div id="timeouts-retries">
  ## Timeouts e tentativas
</div>

Seu endpoint deve responder com um status `2xx` em até **10 segundos**.

Se a entrega falhar (timeout, status não `2xx` ou erro de rede), o Firecrawl fará novas tentativas automaticamente:

| Tentativa | Atraso após falha |
| --------- | ----------------- |
| 1ª        | 1 minuto          |
| 2ª        | 5 minutos         |
| 3ª        | 15 minutos        |

Após 3 tentativas sem sucesso, o webhook é marcado como falho e nenhuma nova tentativa é feita.