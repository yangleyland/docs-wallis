---
title: "Générer des LLMs.txt avec NPX"
description: "Générez des fichiers LLMs.txt depuis n'importe quel site web en CLI avec notre package NPX"
og:title: "Générateur NPX de LLMs.txt | Firecrawl"
og:description: "Transformez des sites web en fichiers texte propres et prêts pour les LLM, via NPX en ligne de commande"
icon: "file-text"
sidebarTitle: "Générer avec NPX"
---

<div id="generate-llmstxt">
  ## generate-llmstxt
</div>

Un simple package NPX qui génère des fichiers llms.txt via la CLI en utilisant l’API Firecrawl. Ce package crée deux fichiers dans le répertoire de sortie spécifié (par défaut, le dossier « public ») :

- `llms.txt` : contient un résumé du contenu lié aux LLM
- `llms-full.txt` : contient le texte intégral

<div id="usage">
  ## Utilisation
</div>

Vous pouvez exécuter ce package avec npx sans l’installer. Il existe deux manières de fournir votre clé d’API Firecrawl :

<div id="1-using-command-line-arguments">
  ### 1. Utiliser des arguments en ligne de commande
</div>

```bash
npx generate-llmstxt --api-key VOTRE_CLÉ_API_FIRECRAWL
```

<div id="2-using-environment-variables">
  ### 2. Utilisation des variables d’environnement
</div>

Créez un fichier `.env` à la racine de votre projet et ajoutez-y votre clé d’API :

```env
FIRECRAWL_API_KEY=your_api_key_here
```

Ensuite, exécutez la commande sans l’option --api-key :

```bash
npx generate-llmstxt
```

<div id="options">
  ### Options
</div>

- `-k, --api-key <key>` (facultatif si configurée dans .env) : votre clé d'API Firecrawl
- `-u, --url <url>` (facultatif) : URL à analyser (par défaut : https://example.com)
- `-m, --max-urls <number>` (facultatif) : nombre maximal d’URL à analyser (par défaut : 50)
- `-o, --output-dir <path>` (facultatif) : chemin du dossier de sortie (par défaut : `public`)

<div id="examples">
  ### Exemples
</div>

```bash
# Utiliser un argument en ligne de commande avec le répertoire de sortie par défaut
npx generate-llmstxt -k your_api_key -u https://your-website.com -m 20

# Utiliser un fichier .env avec le répertoire de sortie par défaut
npx generate-llmstxt -u https://your-website.com -m 20

# Spécifier un répertoire de sortie personnalisé
npx generate-llmstxt -k your_api_key -u https://your-website.com -o custom/path/to/output

# Utiliser un fichier .env et un répertoire de sortie personnalisé
npx generate-llmstxt -u https://your-website.com -o content/llms
```

<div id="requirements">
  ## Prérequis
</div>

- Node.js 14 ou version ultérieure
- Une clé API Firecrawl valide (en ligne de commande ou dans un fichier .env)

<div id="output">
  ## Sortie
</div>

Le package crée deux fichiers dans le répertoire de sortie spécifié (par défaut « public ») :

1. `llms.txt` : contient un résumé du contenu lié aux LLM
2. `llms-full.txt` : contient l’intégralité du contenu textuel

<div id="license">
  ## Licence
</div>

MIT 