---
title: Créer avec l’IA
description: "Tout ce dont vous avez besoin pour intégrer votre agent IA à Firecrawl."
og:title: "Créer avec l’IA | Firecrawl"
og:description: "Tout ce dont vous avez besoin pour intégrer votre agent IA à Firecrawl. Skills, CLI, workflows, MCP, llms.txt, et plus encore."
sidebarTitle: "Créer avec l’IA"
---

Si vous développez avec l’IA, Firecrawl propose plusieurs ressources pour améliorer votre expérience. Firecrawl inclut des **skills** — des blocs de connaissances autonomes que les agents IA de développement découvrent et utilisent automatiquement. Une seule commande d’installation fournit aux agents trois segments complets de skills : des skills CLI pour les opérations web en direct, des skills Build pour intégrer Firecrawl au code applicatif, et des skills workflow pour produire des livrables reproductibles. Les utilisateurs de Firecrawl peuvent obtenir une clé API de deux façons. Voir [Obtenir les identifiants](#get-credentials) ci-dessous.

* [Obtenir les identifiants](#get-credentials)
* [CLI et skills d’agent](#cli)
* [Utiliser Firecrawl comme outil](#using-firecrawl-as-a-tool)
* [Débogage agentique](#agentic-debugging)
* [Serveur MCP Firecrawl](#firecrawl-mcp-server)
* [docs Firecrawl pour les agents](#firecrawl-docs-for-agents)
* [Guides de démarrage rapide](#quick-start-guides)
* [intégration pour agent](#agent-harnesses)
* [SDKs](#sdks)

<div id="get-credentials">
  ## Obtenir les identifiants
</div>

Les utilisateurs de Firecrawl peuvent obtenir une clé API de deux façons. La plupart des utilisateurs doivent se connecter via le dashboard ou la CLI. Si votre plateforme d’agents prend en charge WorkOS ID-JAG, elle peut s’enregistrer directement auprès de Firecrawl.

**Laquelle utiliser ?** Utilisez le dashboard ou la CLI, sauf si vous savez que votre plateforme prend en charge WorkOS ID-JAG.

**Comment vous pouvez arriver ici :**

* **Docs :** vous y êtes déjà. Choisissez l’option ci-dessous qui correspond à votre mode de connexion.
* **API `401` :** les agents compatibles ID-JAG peuvent suivre le flux de découverte dans [`auth.md`](https://www.firecrawl.dev/auth.md), à l’étape 1. Tous les autres doivent utiliser le dashboard ou la CLI.
* **URL directe :** suivez [`SKILL.md`](https://www.firecrawl.dev/agent-onboarding/SKILL.md) pour la connexion et la configuration dans le browser, ou [`auth.md`](https://www.firecrawl.dev/auth.md) pour l’enregistrement WorkOS ID-JAG.

<CardGroup cols={2}>
  <Card title="Utiliser le dashboard ou la CLI" icon="user" href="https://www.firecrawl.dev/agent-onboarding/SKILL.md">
    **Par défaut pour la plupart des utilisateurs.** Connectez-vous depuis le browser, lancez la CLI avec `--browser`, installez les skills et MCP, ou créez une clé API dans le dashboard.
  </Card>

  <Card title="S’enregistrer avec WorkOS ID-JAG" icon="robot" href="/fr/ai-onboarding/agent-auth">
    **Pour les plateformes d’agents prises en charge.** Utilisez cette option lorsque votre plateforme peut émettre un WorkOS ID-JAG pour Firecrawl. Consultez [Agent Auth](/fr/ai-onboarding/agent-auth), puis suivez [`auth.md`](https://www.firecrawl.dev/auth.md).
  </Card>
</CardGroup>

**Vous ne pouvez pas vous connecter ni obtenir de clé ?** La CLI, les SDK et l’API REST permettent d’effectuer des recherches, du scraping, des interactions et du parsing sans clé. Le MCP hébergé n’expose sans clé qu’un périmètre plus restreint : recherche, Scrape et Parse ; Interact y nécessite une connexion à un compte ou une clé API. L’accès sans clé est soumis à une limitation de débit et conçu comme solution de secours ; passez donc à un compte Free ou à une clé API dès que possible. Consultez [Keyless (no API key)](/fr/rate-limits#keyless-no-api-key) pour plus de détails.

Une fois que vous avez une clé API, poursuivez avec la [CLI et les skills d’agent](#cli) ci-dessous.

<span id="cli" />

<div id="cli-and-agent-skills">
  ## CLI et skills d’agent
</div>

Le [Firecrawl CLI](/fr/sdks/cli) permet à votre agent de rechercher, scraper, interagir, lancer un crawl, faire de la cartographie, extraire des données et exécuter des tâches d’agent depuis le terminal. Il est conçu pour les humains, les agents IA et les pipelines CI/CD.

Les **skills** Firecrawl sont des ensembles de connaissances autonomes que les agents IA de développement comme Claude Code, Antigravity et OpenCode découvrent et utilisent automatiquement. Une seule commande d’installation configure tout — les outils CLI pour les opérations web en direct, les skills Build pour intégrer Firecrawl dans le code applicatif et les skills workflow pour produire des livrables reproductibles :

```bash
npx -y firecrawl-cli@latest init --all --browser
```

* `--all` installe chaque segment de skills Firecrawl (CLI, build, workflows) sur tous les agents IA de développement détectés sur la machine
* `--browser` ouvre automatiquement le navigateur pour l’authentification Firecrawl

Après l’installation, vérifiez que tout fonctionne :

```bash
firecrawl --status
firecrawl scrape "https://firecrawl.dev"
```

Pour réinstaller ou cibler un agent spécifique ultérieurement :

```bash
firecrawl setup skills      # CLI + skills Build
firecrawl setup workflows   # skills de workflow
```

<div id="what-the-install-gives-you">
  ### Ce que l’installation inclut
</div>

L’installation met en place trois catégories de skills qui couvrent toutes les façons dont un agent utilise Firecrawl. Chaque segment vit dans son propre dépôt afin de pouvoir évoluer indépendamment :

* [`firecrawl/cli`](https://github.com/firecrawl/cli) — Skills CLI pour les opérations web en direct
* [`firecrawl/skills`](https://github.com/firecrawl/skills) — skills Build pour l’intégration dans l’application
* [`firecrawl/firecrawl-workflows`](https://github.com/firecrawl/firecrawl-workflows) — skills workflow pour des livrables reproductibles

**Skills CLI** — pour les opérations web en direct pendant une session d’agent :

| Skill                | Objectif                                                         |
| -------------------- | ---------------------------------------------------------------- |
| `firecrawl/cli`      | Workflow global des commandes CLI                                |
| `firecrawl-search`   | Rechercher sur le web et découvrir des pages                     |
| `firecrawl-scrape`   | Extraire un contenu propre depuis une URL connue                 |
| `firecrawl-interact` | Interagir avec des pages scrapées à l’aide de prompts ou de code |
| `firecrawl-crawl`    | Extraire en masse le contenu d’un site entier                    |
| `firecrawl-map`      | Découvrir toutes les URL d’un domaine                            |
| `firecrawl-agent`    | Exécuter une collecte autonome de données web avec un job        |

**Skills Build** — pour intégrer Firecrawl au code de l’application :

| Skill                        | Objectif                                                             |
| ---------------------------- | -------------------------------------------------------------------- |
| `firecrawl-build`            | Choisir le bon point de terminaison Firecrawl pour votre produit     |
| `firecrawl-build-onboarding` | Authentification et configuration du projet                          |
| `firecrawl-build-scrape`     | Implémenter le scraping dans le code de l’application                |
| `firecrawl-build-search`     | Implémenter la recherche dans le code de l’application               |
| `firecrawl-build-interact`   | Implémenter l’interaction avec la page dans le code de l’application |
| `firecrawl-build-crawl`      | Implémenter le crawling dans le code de l’application                |
| `firecrawl-build-map`        | Implémenter la découverte d’URL dans le code de l’application        |
| `firecrawl-build-parse`      | Implémenter l’analyse de documents dans le code de l’application     |

**Skills workflow** — des skills orientées résultats qui produisent un livrable concret à partir des données web de Firecrawl :

| Skill                            | Résultat                                                                                                      |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `firecrawl-workflows`            | Skill principale pour choisir le bon workflow                                                                 |
| `firecrawl-deep-research`        | Rapports de recherche multi-sources                                                                           |
| `firecrawl-seo-audit`            | Plans de site, vérifications SEO on-page, comparaison des SERP et correctifs priorisés                        |
| `firecrawl-lead-research`        | Briefs d’intelligence sur les entreprises et les personnes avant réunion                                      |
| `firecrawl-lead-gen`             | Génération de listes de prospects à partir de bases de données et de répertoires                              |
| `firecrawl-qa`                   | Rapports de QA sur site en direct avec problèmes et étapes de reproduction                                    |
| `firecrawl-competitive-intel`    | Monitoring récurrent de la tarification, des fonctionnalités et des journaux des modifications                |
| `firecrawl-market-research`      | Recherche de marché, financière, sur les résultats et sectorielle                                             |
| `firecrawl-research-papers`      | Revues de littérature à partir d’articles, de PDF et de livres blancs                                         |
| `firecrawl-company-directories`  | Extraction de répertoires en listes structurées d’entreprises                                                 |
| `firecrawl-dashboard-reporting`  | Extraction de métriques depuis des dashboards et des outils web internes                                      |
| `firecrawl-knowledge-base`       | Documentation de référence prête pour les LLM, chunks RAG, données d’entraînement ou miroirs de documentation |
| `firecrawl-knowledge-ingest`     | Ingestion de portails de documentation protégés par authentification ou fortement dépendants de JS            |
| `firecrawl-demo-walkthrough`     | Présentations guidées de parcours produit et rapports d’analyse UX                                            |
| `firecrawl-shop`                 | Recherche produit et recommandations d’achat                                                                  |
| `firecrawl-website-design-clone` | Extraire le système de design d’un site web dans un `DESIGN.md` prêt pour un agent                            |

<div id="choose-your-path">
  ### Choisissez votre voie
</div>

Les trois catégories de skills utilisent la même installation. La différence se joue ensuite :

<Steps>
  <Step title="Outils web en temps réel (skills CLI)">
    Utilisez ceci lorsque vous avez besoin de données web pendant votre session en cours — recherche sur le web, scraping d&#39;URL connues, interaction avec des pages scrapées, crawl de documentation, cartographie d&#39;un site ou exécution d&#39;une tâche d’agent.

    Le flux par défaut :

    1. Commencez par **recherche** lorsque vous avez besoin de découverte
    2. Passez à **scrape** lorsque vous avez une URL
    3. Utilisez **interact** lorsque la page scrapée nécessite des actions supplémentaires
    4. Utilisez **cartographie** ou **crawl** lorsque vous avez besoin de nombreuses URL ou pages
    5. Utilisez **agent** lorsque la tâche est ouverte et nécessite une découverte autonome

    ```bash
    # Rechercher sur le web
    firecrawl search "best open-source web crawlers"

    # Scraper une page en markdown propre
    firecrawl scrape https://docs.firecrawl.dev

    # Crawler un site entier
    firecrawl crawl https://docs.firecrawl.dev
    ```
  </Step>

  <Step title="Intégration applicative (skills Build)">
    Utilisez ceci lorsque vous créez une application, un agent ou un workflow qui appelle l&#39;API Firecrawl depuis du code. Les skills Build aident à choisir le bon point de terminaison, à intégrer le SDK et à exécuter un test rapide.

    L&#39;agent répond à une question clé — *que doit faire Firecrawl dans le produit ?* — et les skills Build orientent ensuite vers `/search`, `/scrape`, `/interact`, `/parse`, `/crawl`, `/map` ou `/agent` selon le cas.
  </Step>

  <Step title="Livrables reproductibles (skills Workflow)">
    Utilisez ceci lorsque l&#39;objectif est un livrable finalisé — un rapport de recherche, un audit SEO, un rapport QA, une liste de leads, une base de connaissances, une synthèse de veille concurrentielle ou un système de design cloné — et non des données web brutes ou du code produit.

    Les skills Workflow déduisent d&#39;abord à partir du contexte et ne posent de brèves questions de clarification que lorsqu&#39;une entrée bloquerait le travail. Ils signalent aussi les unités parallélisables de manière indépendante afin que des sous-agents puissent se répartir entre concurrents, pages ou sources.

    Choisissez directement un workflow, ou laissez le skill parapluie `firecrawl-workflows` orienter la requête :

    ```bash
    # Brief de recherche multisource sur un sujet
    "Use firecrawl-deep-research to write a brief on AI agent frameworks"

    # Veille préparatoire avant un appel commercial
    "Use firecrawl-lead-research to brief me on stripe.com before my 3pm call"

    # Audit SEO on-page avec correctifs priorisés
    "Use firecrawl-seo-audit on https://example.com"

    # Cloner le système de design d'un site dans DESIGN.md
    "Use firecrawl-website-design-clone on https://linear.app"
    ```
  </Step>

  <Step title="API REST (aucune installation nécessaire)">
    Si vous préférez ne rien installer, les agents peuvent appeler directement l&#39;API REST de Firecrawl. Définissez la clé API et appelez les points de terminaison :

    * `POST https://api.firecrawl.dev/v2/search` — découvrir des pages à partir d&#39;une requête
    * `POST https://api.firecrawl.dev/v2/scrape` — extraire un markdown propre depuis une URL
    * `POST https://api.firecrawl.dev/v2/scrape/{scrapeId}/interact` — interagir avec une page scrapée
    * `POST https://api.firecrawl.dev/v2/crawl` — extraire en masse un site entier
    * `POST https://api.firecrawl.dev/v2/map` — découvrir des URL sur un domaine
    * `POST https://api.firecrawl.dev/v2/agent` — exécuter une collecte autonome de données web

    En-tête d&#39;authentification : `Authorization: Bearer VOTRE_CLÉ_API`
  </Step>
</Steps>

Les définitions complètes de l&#39;onboarding se trouvent ici :

* Connexion Browser, configuration CLI, skills, MCP et clés du dashboard : [`firecrawl.dev/agent-onboarding/SKILL.md`](https://www.firecrawl.dev/agent-onboarding/SKILL.md)
* Enregistrement d&#39;agent avec WorkOS ID-JAG : [`firecrawl.dev/auth.md`](https://www.firecrawl.dev/auth.md)

Les agents peuvent récupérer directement l&#39;un ou l&#39;autre document : utilisez [`agent-onboarding/SKILL.md`](https://www.firecrawl.dev/agent-onboarding/SKILL.md) pour un onboarding CLI/Browser avec humain dans la boucle, ou [`auth.md`](https://www.firecrawl.dev/auth.md) pour l&#39;enregistrement WorkOS ID-JAG.

<CardGroup cols={3}>
  <Card title="Skills CLI" icon="terminal" href="https://github.com/firecrawl/cli">
    Travail web en temps réel pendant une session d&#39;agent — recherche, scrape, interact, cartographie, crawl et exécution de tâches d’agent depuis le terminal.
  </Card>

  <Card title="Skills Build" icon="code" href="https://github.com/firecrawl/skills">
    Intégrez Firecrawl dans du code applicatif — choisissez le bon point de terminaison, intégrez le SDK et livrez une intégration vérifiée.
  </Card>

  <Card title="Skills Workflow" icon="diagram-project" href="https://github.com/firecrawl/firecrawl-workflows">
    Produisez des livrables reproductibles — briefs de recherche, audits SEO, rapports QA, listes de leads, bases de connaissances et clones de design.
  </Card>
</CardGroup>

<div id="using-firecrawl-as-a-tool">
  ## Utiliser Firecrawl comme outil
</div>

Firecrawl met à la disposition des agents cinq outils principaux pour travailler avec le web. Chaque outil correspond à un point de terminaison d’API et à une commande CLI. Les agents choisissent l’outil adapté à leurs besoins :

<AccordionGroup>
  <Accordion title="recherche — découvrir des pages à partir d’une requête" icon="magnifying-glass">
    Commencez ici si vous n’avez pas encore d’URL. recherche renvoie des pages web pertinentes pour une requête en langage naturel, avec en option le contenu complet des pages dans les résultats.

    ```bash
    # CLI
    firecrawl search "latest OpenAI API pricing"
    ```

    ```bash
    # API REST
    curl -X POST https://api.firecrawl.dev/v2/search \
      -H "Authorization: Bearer fc-YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{"query": "latest OpenAI API pricing"}'
    ```

    **Quand l’utiliser :** recherche, localisation de documentation, analyse concurrentielle, réponse à des questions nécessitant des informations web à jour.
  </Accordion>

  <Accordion title="Scrape — extraire le contenu d’une URL" icon="file-lines">
    Utilisez cet outil lorsque vous avez déjà une URL et que vous avez besoin d’un contenu propre, prêt pour les LLM. Scrape convertit n’importe quelle page web en markdown, HTML ou données structurées, en gérant automatiquement le rendu JavaScript, les mesures anti-bot et le HTML désordonné.

    ```bash
    # CLI
    firecrawl scrape https://docs.stripe.com/api/charges
    ```

    ```bash
    # API REST
    curl -X POST https://api.firecrawl.dev/v2/scrape \
      -H "Authorization: Bearer fc-YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{"url": "https://docs.stripe.com/api/charges"}'
    ```

    **Quand l’utiliser :** lecture de documentation, extraction du contenu d’articles, récupération de données depuis une page connue, conversion de pages web en contexte pour les LLM.
  </Accordion>

  <Accordion title="Crawl — extraire en masse un site entier" icon="spider">
    Crawl suit récursivement les liens à partir d’une URL de départ et scrape chaque page trouvée. Utilisez-le lorsque vous avez besoin du contenu d’un site entier ou d’un ensemble de documentation, et non d’une seule page.

    ```bash
    # CLI
    firecrawl crawl https://docs.firecrawl.dev --limit 50
    ```

    ```bash
    # API REST
    curl -X POST https://api.firecrawl.dev/v2/crawl \
      -H "Authorization: Bearer fc-YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{"url": "https://docs.firecrawl.dev", "limit": 50}'
    ```

    **Quand l’utiliser :** ingestion de sites de documentation complets, création de bases de connaissances, migration de contenu, collecte de données d’entraînement.
  </Accordion>

  <Accordion title="cartographie — découvrir toutes les URL d’un domaine" icon="sitemap">
    cartographie découvre rapidement toutes les URL indexées d’un domaine sans en scraper le contenu. Utilisez-le lorsque vous avez besoin de comprendre la structure d’un site ou de trouver des pages spécifiques avant de les scraper.

    ```bash
    # CLI
    firecrawl map https://docs.firecrawl.dev
    ```

    ```bash
    # API REST
    curl -X POST https://api.firecrawl.dev/v2/map \
      -H "Authorization: Bearer fc-YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{"url": "https://docs.firecrawl.dev"}'
    ```

    **Quand l’utiliser :** audits de site, recherche de pages spécifiques sur un site volumineux, compréhension de la structure d’un site avant un crawl ciblé.
  </Accordion>

  <Accordion title="Interact — travailler avec une page scrapée" icon="hand-pointer">
    Interact permet aux agents de poursuivre à partir d’un scrape à l’aide de prompts ou de code. Utilisez-le lorsqu’une page scrapée nécessite des clics, le remplissage de formulaires, de la navigation ou une extraction complémentaire.

    ```bash
    # CLI
    firecrawl scrape https://example.com
    firecrawl interact "Click the pricing tab and extract the plan names"
    ```

    ```bash
    # API REST
    # scrapeId provient de la réponse de scrape (data.metadata.scrapeId)
    curl -X POST https://api.firecrawl.dev/v2/scrape/SCRAPE_ID/interact \
      -H "Authorization: Bearer fc-YOUR_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{"prompt": "Click the pricing tab and extract the plan names"}'
    ```

    **Quand l’utiliser :** poursuite après un scrape, navigation dans des pages dynamiques, remplissage de formulaires et extraction de données après des actions sur la page.
  </Accordion>
</AccordionGroup>

<div id="how-agents-chain-tools-together">
  ### Comment les agents enchaînent les outils
</div>

La plupart des workflows d’agents combinent plusieurs outils. Schéma typique :

1. **Recherche** de pages pertinentes → obtenir une liste d’URL
2. **Scrape** des URL les plus pertinentes → obtenir un contenu propre
3. **Interact** lorsque la page scrapée nécessite des actions de suivi
4. **Agent** lorsque la tâche nécessite une découverte autonome ou une extraction structurée sur plusieurs pages

Pour les traitements en masse, les agents utilisent d’abord **Cartographie** pour découvrir des URL, puis **Crawl** ou **Scrape** de manière sélective sur les pages dont ils ont besoin.

<div id="agentic-debugging">
  ## Débogage agentique
</div>

Lorsqu&#39;un appel à Firecrawl échoue ou renvoie des résultats inattendus, votre agent n&#39;a pas besoin de faire appel à un humain. Le point de terminaison [`/support/ask`](/fr/api-reference/endpoint/ask) est un agent de support IA conçu pour la communication **d’agent à agent** — il diagnostique les problèmes liés à vos jobs, à votre compte et à votre utilisation de l’API, puis renvoie une réponse vérifiée avec des paramètres de correction lisibles par machine que votre agent peut appliquer directement.

Intégrez-le au flux de gestion des erreurs de votre agent afin qu’il puisse se rétablir automatiquement après des échecs de scraping, des problèmes de crawl et des erreurs de configuration — généralement en 15 à 30 secondes, sans intervention humaine.

<div id="how-it-works">
  ### Comment cela fonctionne
</div>

1. **Votre agent décrit le problème** — une question en langage naturel qui décrit le problème.
2. **L&#39;agent de support enquête** — il examine les logs de tâche, l&#39;état du compte, la documentation et le code source.
3. **L&#39;agent de support valide** — lorsque c&#39;est possible, il teste un correctif sur l&#39;API Firecrawl en conditions réelles (par exemple, en relançant un scrape avec des paramètres ajustés).
4. **Votre agent obtient une réponse vérifiée** — une `answer` rédigée en langage naturel, des `fixParameters` exploitables par machine à appliquer directement, et des résultats de `validation` indiquant si le correctif a été testé.

<div id="example">
  ### Exemple
</div>

Envoyez une question, ainsi qu’un `rationale` facultatif pour donner à l’agent de support plus de contexte sur l’objectif de votre utilisateur final :

```bash
curl -X POST https://api.firecrawl.dev/v2/support/ask \
  -H "Authorization: Bearer fc-YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "question": "my crawl returned 3 pages but I expected 50",
    "rationale": "user is on their third failed crawl attempt today"
  }'
```

La réponse inclut une `answer`, un niveau de `confidence`, des `fixParameters` facultatifs (par ex. `{"waitFor": 5000}`) que votre agent peut transmettre lors de l’appel suivant, ainsi qu’une `validation` indiquant si le correctif a été testé sur l’API en production.

<Card title="Référence du point de terminaison Ask" icon="life-ring" href="/fr/api-reference/endpoint/ask">
  Schéma complet de la requête et de la réponse pour `/support/ask`, y compris les codes d’état et l’enveloppe de retour renvoyée lorsque l’agent est bloqué.
</Card>

<div id="firecrawl-mcp-server">
  ## Firecrawl MCP Server
</div>

MCP est un protocole ouvert qui permet aux applications compatibles de donner aux modèles accès à des outils. Le [serveur MCP Firecrawl](https://github.com/firecrawl/firecrawl-mcp-server) open source prend en charge la recherche, le scraping, la navigation, le crawl, la cartographie, l’extraction structurée via Scrape JSON et les tâches d’agent autonomes.

<Card title="Choisir une connexion MCP" icon="plug" href="/fr/mcp-server">
  Essayez l’accès sans clé, connectez-vous via un client interactif ou configurez une clé API pour les agents, l’intégration continue et les services backend.
</Card>

<div id="firecrawl-docs-for-agents">
  ## Docs Firecrawl pour les agents
</div>

Vous pouvez fournir à votre agent la documentation Firecrawl à jour de façon contextualisée. Les agents peuvent se configurer eux-mêmes en récupérant directement ces ressources — sans intervention humaine.

<Steps>
  <Step title="Obtenir les identifiants">
    Démarrez par [Obtenir les identifiants](/fr/ai-onboarding#get-credentials) pour choisir entre la configuration via le dashboard/CLI et l’enregistrement à WorkOS ID-JAG. Pour l’enregistrement ID-JAG, suivez [`auth.md`](https://www.firecrawl.dev/auth.md).
  </Step>

  <Step title="Docs Markdown">
    Chaque page dispose d&#39;une version markdown. Ajoutez `.md` à n&#39;importe quelle URL de la documentation, ou utilisez le menu d&#39;action de la page pour la copier au format markdown.

    ```
    Docs for this page: https://docs.firecrawl.dev/ai-onboarding.md
    ```
  </Step>

  <Step title="llms.txt complet">
    Fournissez à votre agent l&#39;ensemble de notre documentation dans un seul fichier.

    ```
    Here are the Firecrawl docs: https://docs.firecrawl.dev/llms-full.txt
    ```

    Une version d&#39;index plus courte est également disponible à `https://docs.firecrawl.dev/llms.txt`.
  </Step>

  <Step title="Serveur de docs MCP">
    Pour une approche structurée avec les outils MCP, connectez le serveur MCP Firecrawl dans Codex, Cursor, Claude Code, Claude Desktop ou un autre client compatible. Commencez par le [sélecteur de configuration MCP](/fr/mcp-server). Les utilisateurs de Windsurf doivent suivre le [démarrage rapide de Windsurf](/fr/quickstarts/windsurf).
  </Step>

  <Step title="Copier vers ChatGPT / Claude">
    Chaque page inclut un menu d&#39;action contextuel (copier, consulter en markdown, ouvrir dans ChatGPT, ouvrir dans Claude), afin que les agents comme les humains puissent déplacer des pages entre les outils en un clic.
  </Step>
</Steps>

<div id="quick-start-guides">
  ## Guides de démarrage rapide
</div>

Des guides de démarrage rapide prêts à l’emploi pour les stacks sur lesquelles les agents s’appuient le plus souvent. Faites pointer votre agent vers l’un d’eux pour mettre en place une intégration Firecrawl complète et fonctionnelle.

Vous préférez laisser Cursor s’en charger ? Installez en un clic le serveur MCP Firecrawl et commencez à rédiger des prompts dans Cursor :

<a href="cursor://anysphere.cursor-deeplink/mcp/install?name=firecrawl&config=eyJ1cmwiOiJodHRwczovL21jcC5maXJlY3Jhd2wuZGV2L3YyL21jcCJ9">
  <img src="https://cursor.com/deeplink/mcp-install-dark.png" alt="Ouvrir dans Cursor — Ajouter le serveur MCP Firecrawl" style={{ maxHeight: 32 }} />
</a>

<CardGroup cols={3}>
  <Card title="Node.js" icon="node-js" href="/fr/quickstarts/nodejs">
    JavaScript et TypeScript côté serveur avec le Firecrawl Node SDK.
  </Card>

  <Card title="Next.js" icon="react" href="/fr/quickstarts/nextjs">
    Scraper, rechercher et lancer un crawl depuis les route handlers et server actions de Next.js.
  </Card>

  <Card title="Python" icon="python" href="/fr/quickstarts/python">
    Utilisez Firecrawl depuis des scripts, des notebooks et des services backend.
  </Card>

  <Card title="FastAPI" icon="bolt" href="/fr/quickstarts/fastapi">
    Créez des API Python asynchrones pour la recherche, le scrape et l’extraction.
  </Card>

  <Card title="Cloudflare Workers" icon="cloudflare" href="/fr/quickstarts/cloudflare-workers">
    Exécutez Firecrawl à l’edge avec Workers.
  </Card>

  <Card title="Vercel Functions" icon="triangle" href="/fr/quickstarts/vercel-functions">
    Appelez Firecrawl depuis des fonctions serverless Vercel.
  </Card>

  <Card title="AWS Lambda" icon="aws" href="/fr/quickstarts/aws-lambda">
    Invoquez Firecrawl depuis des handlers Lambda.
  </Card>

  <Card title="Supabase Edge Functions" icon="database" href="/fr/quickstarts/supabase-edge-functions">
    Utilisez Firecrawl dans l’environnement d’exécution Deno de Supabase.
  </Card>

  <Card title="Go" icon="golang" href="/fr/quickstarts/go">
    SDK Go idiomatique pour la recherche, le scrape et le crawl.
  </Card>

  <Card title="Rust" icon="rust" href="/fr/quickstarts/rust">
    SDK Rust typé pour Firecrawl.
  </Card>

  <Card title="Laravel" icon="laravel" href="/fr/quickstarts/laravel">
    Ajoutez Firecrawl à vos applications Laravel via le SDK PHP.
  </Card>

  <Card title="Rails" icon="gem" href="/fr/quickstarts/rails">
    Intégrez Firecrawl à Ruby on Rails.
  </Card>
</CardGroup>

Consultez la liste complète des guides de démarrage rapide (Express, NestJS, Fastify, Hono, Bun, Remix, Nuxt, SvelteKit, Astro, Mastra, Django, Flask, Elixir, Java, Spring Boot, .NET, ASP.NET Core, et plus encore) dans la barre latérale gauche.

<div id="agent-harnesses">
  ## Intégrations pour agents
</div>

Firecrawl s’intègre aux environnements d’exécution et frameworks que les agents utilisent réellement — agents de codage, SDK d’agents et agrégateurs de modèles. La plupart des outils de codage peuvent détecter automatiquement les skills Firecrawl via `npx -y firecrawl-cli@latest init --all --browser` ; les autres appellent Firecrawl comme outil via MCP ou l’API REST.

<CardGroup cols={3}>
  <Card title="Claude Code" icon="terminal" href="/fr/quickstarts/claude-code">
    CLI d’Anthropic — configurez Firecrawl MCP dans Claude Code.
  </Card>

  <Card title="Cursor" icon="arrow-pointer" href="/fr/quickstarts/cursor">
    IDE agentique — installez Firecrawl MCP dans Cursor en un clic.
  </Card>

  <Card title="OpenCode" icon="code-branch" href="/fr/quickstarts/opencode">
    Intégrez Firecrawl MCP à OpenCode.
  </Card>

  <Card title="Codex CLI" icon="code" href="/fr/quickstarts/codex-cli">
    Intégrez Firecrawl MCP à OpenAI Codex CLI.
  </Card>

  <Card title="OpenRouter" icon="route" href="/fr/quickstarts/openrouter">
    Associez n’importe quel modèle OpenRouter aux outils web de Firecrawl.
  </Card>

  <Card title="Amp" icon="bolt" href="/fr/quickstarts/amp">
    Intégrez Firecrawl MCP à Sourcegraph Amp.
  </Card>

  <Card title="Windsurf" icon="wind" href="/fr/quickstarts/windsurf">
    IDE agentique — configurez Firecrawl MCP dans Windsurf.
  </Card>

  <Card title="Antigravity" icon="rocket" href="/fr/quickstarts/antigravity">
    Ajoutez Firecrawl MCP à l’IDE agentique de Google.
  </Card>

  <Card title="Gemini CLI" icon="gem" href="/fr/quickstarts/gemini-cli">
    Intégrez Firecrawl MCP à Google Gemini CLI.
  </Card>

  <Card title="Nous Research" icon="brain" href="/fr/quickstarts/nous-research">
    Utilisez Firecrawl comme outil avec les modèles Hermes.
  </Card>

  <Card title="AutoGen" icon="robot" href="/fr/quickstarts/autogen">
    Utilisez les outils Firecrawl au sein des équipes multi-agents de Microsoft AutoGen.
  </Card>
</CardGroup>

<div id="sdks">
  ## SDKs
</div>

Des SDKs officiels et typés couvrant l’ensemble de l’API Firecrawl. Choisissez pour votre agent le langage adapté à votre stack.

<CardGroup cols={3}>
  <Card title="Python" icon="python" href="/fr/sdks/python" />

  <Card title="Node" icon="node-js" href="/fr/sdks/node" />

  <Card title="Go" icon="golang" href="/fr/sdks/go" />

  <Card title="Java" icon="java" href="/fr/sdks/java" />

  <Card title="Ruby" icon="gem" href="/fr/sdks/ruby" />

  <Card title="Rust" icon="rust" href="/fr/sdks/rust" />

  <Card title=".NET" icon="microsoft" href="/fr/sdks/dotnet" />

  <Card title="PHP" icon="php" href="/fr/sdks/php" />

  <Card title="Elixir" icon="droplet" href="/fr/sdks/elixir" />

  <Card title="CLI" icon="terminal" href="/fr/sdks/cli" />
</CardGroup>

Firecrawl propose également des intégrations SDK natives pour les principaux SDK de LLM et frameworks d’agents — consultez [LLM SDKs and Frameworks](/fr/developer-guides/llm-sdks-and-frameworks/openai) pour OpenAI, Anthropic, Gemini, Google ADK, Vercel AI SDK, LangChain, LangGraph, LlamaIndex, Mastra et ElevenAgents.