---
title: 'Rust'
description: "Le SDK Rust de Firecrawl est une bibliothèque qui facilite le scraping et le crawling de sites web et génère des données prêtes à l'emploi pour les modèles de langage (LLM)."
icon: 'rust'
og:title: "Rust SDK | Firecrawl"
og:description: "Le SDK Rust de Firecrawl est une bibliothèque qui facilite le scraping et le crawling de sites web et génère des données prêtes à l'emploi pour les modèles de langage (LLM)."
---

import InstallationRust from '/snippets/fr/v1/installation/rust.mdx'
import ScrapeAndCrawlRustExample from '/snippets/fr/v1/scrape-and-crawl/rust.mdx'
import ScrapeRustShort from '/snippets/fr/v1/scrape/short/rust.mdx'
import CrawlRustShort from '/snippets/fr/v1/crawl/short/rust.mdx'
import CrawlAsyncRustShort from '/snippets/fr/v1/crawl-async/short/rust.mdx'
import MapRustShort from '/snippets/fr/v1/map/short/rust.mdx'
import LLMExtractRust from '/snippets/fr/v1/llm-extract/base/rust.mdx'

<Warning>
  Ce SDK utilise actuellement la version **v1** de l’API Firecrawl, qui n’est pas la plus récente (la v2 est disponible). Certaines fonctionnalités et améliorations peuvent n’être proposées qu’en v2.
</Warning>

<div id="installation">
  ## Installation
</div>

Pour installer le SDK Firecrawl pour Rust, ajoutez ce qui suit à votre `Cargo.toml` :

<InstallationRust />

<div id="usage">
  ## Utilisation
</div>

Commencez par obtenir une clé API sur [firecrawl.dev](https://firecrawl.dev). Initialisez ensuite `FirecrawlApp`. Vous pourrez alors accéder à des fonctions comme `FirecrawlApp::scrape_url`, qui vous permettent d'utiliser notre API.

Voici un exemple d'utilisation du SDK en Rust :

<ScrapeAndCrawlRustExample />

<div id="scraping-a-url">
  ### Récupérer le contenu d’une URL
</div>

Pour traiter une seule URL, utilisez la méthode `scrape_url`. Elle prend l’URL en paramètre et renvoie les données collectées sous forme de `Document`.

<ScrapeRustShort />

<div id="scraping-with-extract">
  ### Extraction avec Extract
</div>

Avec Extract, vous pouvez facilement extraire des données structurées à partir de n’importe quelle URL. Vous devez définir votre schéma au format JSON Schema, en utilisant la macro `serde_json::json!`.

<LLMExtractRust />

<div id="crawling-a-website">
  ### Explorer un site web
</div>

Pour explorer un site web, utilisez la méthode `crawl_url`. Elle attendra la fin de l’exploration, ce qui peut prendre un certain temps selon l’URL de départ et les options choisies.

<CrawlRustShort />

<div id="crawling-asynchronously">
  #### Exploration asynchrone
</div>

Pour explorer sans attendre le résultat, utilisez la méthode `crawl_url_async`. Elle accepte les mêmes paramètres, mais renvoie une structure `CrawlAsyncRespone` contenant l’ID de l’exploration. Vous pouvez utiliser cet ID avec la méthode `check_crawl_status` pour vérifier l’état à tout moment. Notez que les explorations terminées sont supprimées après 24 heures.

<CrawlAsyncRustShort />

<div id="map-a-url">
  ### Cartographier une URL
</div>

Répertorier tous les liens associés à partir d’une URL de départ.

<MapRustShort />

<div id="error-handling">
  ## Gestion des erreurs
</div>

Le SDK gère les erreurs renvoyées par l’API Firecrawl et par nos dépendances, et les regroupe dans l’énumération `FirecrawlError`, qui implémente `Error`, `Debug` et `Display`. Toutes nos méthodes retournent un `Result<T, FirecrawlError>`.