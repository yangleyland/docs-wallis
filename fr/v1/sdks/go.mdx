---
title: 'Go'
description: 'Le SDK Go de Firecrawl est une surcouche de l’API Firecrawl qui vous aide à convertir facilement des sites web en Markdown.'
icon: 'golang'
og:title: "Go SDK | Firecrawl"
og:description: "Le SDK Go de Firecrawl est une surcouche de l’API Firecrawl qui vous aide à convertir facilement des sites web en Markdown."
---

import InstallGoSDK from "/snippets/fr/v1/installation/go.mdx";
import ScrapeAndCrawlGoExample from "/snippets/fr/v1/scrape-and-crawl/go.mdx";
import ScrapeGoShort from "/snippets/fr/v1/scrape/short/go.mdx";
import CrawlGoShort from "/snippets/fr/v1/crawl/short/go.mdx";
import CheckCrawlStatusGoShort from "/snippets/fr/v1/crawl-status/short/go.mdx";
import MapGoShort from "/snippets/fr/v1/map/short/go.mdx";

<Warning>
  Ce SDK utilise actuellement la version **v1** de l’API Firecrawl, qui n’est pas la plus récente (la v2 est disponible). Certaines fonctionnalités et améliorations peuvent n’être disponibles qu’en v2.
</Warning>

<div id="installation">
  ## Installation
</div>

Pour installer le SDK Go de Firecrawl, utilisez go get :

<InstallGoSDK />

<div id="usage">
  ## Utilisation
</div>

1. Récupérez une clé API sur [firecrawl.dev](https://firecrawl.dev)
2. Passez la `clé API` en paramètre à la structure `FirecrawlApp`.
3. Définissez l’`URL de l’API` et/ou passez-la en paramètre à la structure `FirecrawlApp`. Valeur par défaut : `https://api.firecrawl.dev`.
4. Définissez la `version` et/ou passez-la en paramètre à la structure `FirecrawlApp`. Valeur par défaut : `v1`.

Voici un exemple d’utilisation du SDK avec gestion des erreurs :

<ScrapeAndCrawlGoExample />

<div id="scraping-a-url">
  ### Extraire une URL
</div>

Pour extraire une URL unique avec gestion des erreurs, utilisez la méthode `ScrapeURL`. Elle prend l’URL en paramètre et renvoie les données extraites sous forme de dictionnaire.

<ScrapeGoShort />

<div id="crawling-a-website">
  ### Explorer un site web
</div>

Pour explorer un site web, utilisez la méthode `CrawlUrl`. Elle prend comme arguments l’URL de départ et des paramètres optionnels. L’argument `params` permet de définir des options supplémentaires pour la tâche d’exploration, comme le nombre maximal de pages à parcourir, les domaines autorisés et le format de sortie.

<CrawlGoShort />

<div id="checking-crawl-status">
  ### Vérifier l’état du crawl
</div>

Pour consulter l’état d’une tâche de crawl, utilisez la méthode `CheckCrawlStatus`. Elle prend l’ID de la tâche en paramètre et renvoie l’état actuel du crawl.

<CheckCrawlStatusGoShort />

<div id="map-a-website">
  ### Cartographier un site web
</div>

Utilisez `MapUrl` pour générer une liste d’URL à partir d’un site web. L’argument `params` vous permet de personnaliser la cartographie, notamment pour exclure les sous-domaines ou exploiter le sitemap.

<MapGoShort />

<div id="error-handling">
  ## Gestion des erreurs
</div>

Le SDK gère les erreurs renvoyées par l’API Firecrawl et déclenche des exceptions appropriées. En cas d’erreur lors d’une requête, une exception est levée avec un message d’erreur explicite.