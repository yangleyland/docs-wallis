---
title: "Recherche"
description: "Recherchez sur le web et obtenez le contenu complet des résultats"
og:title: "Recherche | Firecrawl"
og:description: "Recherchez sur le web et obtenez le contenu intégral des pages depuis les résultats"
icon: "magnifying-glass"
---

import InstallationPython from "/snippets/fr/v1/installation/python.mdx";
import InstallationNode from "/snippets/fr/v1/installation/js.mdx";
import InstallationGo from "/snippets/fr/v1/installation/go.mdx";
import InstallationRust from "/snippets/fr/v1/installation/rust.mdx";
import SearchPython from "/snippets/fr/v1/search/base/python.mdx";
import SearchNode from "/snippets/fr/v1/search/base/js.mdx";
import SearchCURL from "/snippets/fr/v1/search/base/curl.mdx";
import SearchContentPython from "/snippets/fr/v1/search/content/python.mdx";
import SearchContentNode from "/snippets/fr/v1/search/content/js.mdx";
import SearchContentCURL from "/snippets/fr/v1/search/content/curl.mdx";
import SearchLocationPython from "/snippets/fr/v1/search/location/python.mdx";
import SearchLocationNode from "/snippets/fr/v1/search/location/js.mdx";
import SearchLocationCURL from "/snippets/fr/v1/search/location/curl.mdx";
import SearchTimePython from "/snippets/fr/v1/search/time/python.mdx";
import SearchTimeNode from "/snippets/fr/v1/search/time/js.mdx";
import SearchTimeCURL from "/snippets/fr/v1/search/time/curl.mdx";

L’API de recherche de Firecrawl vous permet d’effectuer des recherches sur le web et, en option, de récupérer et d’extraire les résultats en une seule opération.

* Choisissez des formats de sortie précis (markdown, HTML, liens, captures d’écran)
* Lancez des recherches web avec des paramètres personnalisables (localisation, etc.)
* Récupérez en option le contenu des résultats de recherche dans différents formats
* Contrôlez le nombre de résultats et définissez des délais d’attente

Pour plus de détails, consultez la [référence du point de terminaison /search](https://docs.firecrawl.dev/api-reference/endpoint/search).

<div id="performing-a-search-with-firecrawl">
  ## Effectuer une recherche avec Firecrawl
</div>

<div id="search-endpoint">
  ### point de terminaison /search
</div>

Permet d’effectuer des recherches sur le web et, en option, de récupérer le contenu des résultats.

<div id="installation">
  ### Installation
</div>

<CodeGroup>

  <InstallationPython />

  <InstallationNode />

  <InstallationGo />
  
  <InstallationRust />

</CodeGroup>

<div id="basic-usage">
  ### Utilisation basique
</div>

<CodeGroup>

  <SearchPython />

  <SearchNode />
  
  <SearchCURL />

</CodeGroup>

<div id="response">
  ### Réponse
</div>

Les SDK renverront directement l’objet de données. cURL renverra le payload complet.

```json
{
  "success": true,
  "data": [
    {
      "title": "Firecrawl - L’API de web scraping ultime",
      "description": "Firecrawl est une API de web scraping puissante qui transforme n’importe quel site web en données propres et structurées pour l’IA et l’analyse.",
      "url": "https://firecrawl.dev/"
    },
    {
      "title": "Web scraping avec Firecrawl - Guide complet",
      "description": "Découvrez comment utiliser Firecrawl pour extraire des données de sites web en toute simplicité.",
      "url": "https://firecrawl.dev/guides/web-scraping/"
    },
    {
      "title": "Documentation Firecrawl - Démarrage",
      "description": "Documentation officielle de l’API de web scraping Firecrawl.",
      "url": "https://docs.firecrawl.dev/"
    }
    // ... plus de résultats
  ]
}
```

<div id="search-with-content-scraping">
  ## Rechercher avec extraction de contenu
</div>

Effectuez une recherche et récupérez le contenu des résultats en une seule opération.

<CodeGroup>

  <SearchContentPython />

  <SearchContentNode />
  
  <SearchContentCURL />

</CodeGroup>

<div id="response-with-scraped-content">
  ### Réponse avec contenu collecté
</div>

```json
{
  "success": true,
  "data": [
    {
      "title": "Firecrawl - L’API de web scraping ultime",
      "description": "Firecrawl est une API de web scraping puissante qui convertit n’importe quel site web en données propres et structurées pour l’IA et l’analyse.",
      "url": "https://firecrawl.dev/",
      "markdown": "# Firecrawl\n\nL’API de web scraping ultime\n\n## Convertissez n’importe quel site web en données propres et structurées\n\nFirecrawl simplifie l’extraction de données à partir de sites web pour des applications d’IA, des études de marché, l’agrégation de contenu, et bien plus encore...",
      "links": [
        "https://firecrawl.dev/pricing",
        "https://firecrawl.dev/docs",
        "https://firecrawl.dev/guides",
        // ... plus de liens
      ],
      "metadata": {
        "title": "Firecrawl - L’API de web scraping ultime",
        "description": "Firecrawl est une API de web scraping puissante qui convertit n’importe quel site web en données propres et structurées pour l’IA et l’analyse.",
        "sourceURL": "https://firecrawl.dev/",
        "statusCode": 200
      }
    },
    // ... plus de résultats
  ]
}
```

<div id="advanced-search-options">
  ## Options de recherche avancées
</div>

L’API de recherche de Firecrawl prend en charge différents paramètres pour personnaliser votre recherche :

<div id="location-customization">
  ### Personnalisation de la localisation
</div>

<CodeGroup>

  <SearchLocationPython />

  <SearchLocationNode />
  
  <SearchLocationCURL />

</CodeGroup>

<div id="time-based-search">
  ### Recherche par période
</div>

Utilisez le paramètre `tbs` pour filtrer les résultats selon la période :

<CodeGroup>
  <SearchTimePython />

  <SearchTimeNode />

  <SearchTimeCURL />
</CodeGroup>

Valeurs `tbs` courantes :

* `qdr:h` - Dernière heure
* `qdr:d` - Dernières 24 heures
* `qdr:w` - Dernière semaine
* `qdr:m` - Dernier mois
* `qdr:y` - Dernière année

Pour un filtrage temporel plus précis, vous pouvez indiquer des plages de dates exactes en utilisant le format de plage personnalisé :

<CodeGroup>
  ```python Python
  from firecrawl import Firecrawl

  # Initialiser le client avec votre clé API
  app = Firecrawl(api_key="VOTRE_CLÉ_API")

  # Rechercher des résultats de décembre 2024
  search_result = app.search(
      "firecrawl updates",
      limit=10,
      tbs="cdr:1,cd_min:12/1/2024,cd_max:12/31/2024"
  )
  ```

  ```js JavaScript
  import { Firecrawl } from 'firecrawl';

  // Initialiser le client avec votre clé API
  const app = new Firecrawl({apiKey: "VOTRE_CLÉ_API"});

  // Rechercher des résultats de décembre 2024
  app.search("firecrawl updates", {
    limit: 10,
    tbs: "cdr:1,cd_min:12/1/2024,cd_max:12/31/2024"
  })
  .then(searchResult => {
    console.log(searchResult.data);
  });
  ```

  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v1/search \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer VOTRE_CLÉ_API" \
    -d '{
      "query": "firecrawl updates",
      "limit": 10,
      "tbs": "cdr:1,cd_min:12/1/2024,cd_max:12/31/2024"
    }'
  ```
</CodeGroup>

<div id="custom-timeout">
  ### Délai d’attente personnalisé
</div>

Définissez un délai d’attente personnalisé pour les opérations de recherche :

<CodeGroup>
  ```python Python
  from firecrawl import Firecrawl

  # Initialisez le client avec votre clé API
  app = Firecrawl(api_key="VOTRE_CLÉ_API")

  # Définissez un délai d’attente de 30 secondes
  search_result = app.search(
      "complex search query",
      limit=10,
      timeout=30000  # 30 secondes en millisecondes
  )
  ```

  ```js JavaScript
  import { Firecrawl } from 'firecrawl';

  // Initialisez le client avec votre clé API
  const app = new Firecrawl({apiKey: "VOTRE_CLÉ_API"});

  // Définissez un délai d’attente de 30 secondes
  app.search("complex search query", {
    limit: 10,
    timeout: 30000  // 30 secondes en millisecondes
  })
  .then(searchResult => {
    // Traitez les résultats
    console.log(searchResult.data);
  });
  ```

  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v1/search \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer VOTRE_CLÉ_API" \
    -d '{
      "query": "complex search query",
      "limit": 10,
      "timeout": 30000
    }'
  ```
</CodeGroup>

<div id="scraping-options">
  ## Options de scraping
</div>

Lors du scraping de résultats de recherche, vous pouvez spécifier plusieurs formats de sortie et des options de scraping avancées :

<CodeGroup>
  ```python Python
  from firecrawl import Firecrawl, ScrapeOptions

  # Initialize the client with your API key
  app = Firecrawl(api_key="VOTRE_CLÉ_API")

  # Get search results with multiple formats
  search_result = app.search(
      "firecrawl features",
      limit=3,
      scrape_options=ScrapeOptions(formats=["markdown", "html", "links", "screenshot"])
  )
  ```

  ```js JavaScript
  import { Firecrawl } from 'firecrawl';

  // Initialize the client with your API key
  const app = new Firecrawl({apiKey: "VOTRE_CLÉ_API"});

  // Get search results with multiple formats
  app.search("firecrawl features", {
    limit: 3,
    scrapeOptions: {
      formats: ["markdown", "html", "links", "screenshot"]
    }
  })
  .then(searchResult => {
    // Process results with various formats
    console.log(searchResult.data);
  });
  ```

  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v1/search \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer VOTRE_CLÉ_API" \
    -d '{
      "query": "firecrawl features",
      "limit": 3,
      "scrapeOptions": {
        "formats": ["markdown", "html", "links", "screenshot"]
      }
    }'
  ```
</CodeGroup>

Formats disponibles :

* `markdown` : Contenu Markdown propre et mis en forme
* `html` : Contenu HTML traité
* `rawHtml` : Contenu HTML non modifié
* `links` : Liste des liens trouvés sur la page
* `screenshot` : Capture d’écran de la page
* `screenshot@fullPage` : Capture d’écran de la page entière
* `extract` : Extraction de données structurées

<div id="cost-implications">
  ## Impact sur les coûts
</div>

Le coût d’une recherche est de 2 crédits pour 10 résultats de recherche. Si les options de scraping sont activées, les coûts de scraping standard s’appliquent à chaque résultat de recherche :

- **Basic scrape**: 1 crédit par page web
- **PDF parsing**: 1 crédit par page PDF
- **Enhanced proxy mode**: 4 crédits supplémentaires par page web
- **JSON mode**: 4 crédits supplémentaires par page web

Pour maîtriser les coûts :

- Définissez `parsers: []` si l’analyse de PDF n’est pas nécessaire
- Utilisez `proxy: "basic"` au lieu de `"enhanced"` lorsque c’est possible, ou réglez-le sur `"auto"`
- Limitez le nombre de résultats de recherche avec le paramètre `limit`

<div id="advanced-scraping-options">
  ## Options de scraping avancées
</div>

Pour plus de détails sur les options de scraping, consultez la [documentation de la fonctionnalité Scrape](https://docs.firecrawl.dev/features/scrape). Tout, à l’exception de FIRE-1 (Agent) et du suiviDesModifications, est pris en charge par ce point de terminaison Search.