---
title: 'mode JSON - Extraction par LLM'
description: 'Extraire des données structurées à partir de pages via des LLM'
og:title: "mode JSON | Firecrawl"
og:description: "Extraire des données structurées à partir de pages via des LLM"
sidebarTitle: "mode JSON"
---

import ExtractCURL from "/snippets/fr/v1/llm-extract/base/curl.mdx";
import ExtractPython from "/snippets/fr/v1/llm-extract/base/python.mdx";
import ExtractNode from "/snippets/fr/v1/llm-extract/base/js.mdx";
import ExtractOutput from "/snippets/fr/v1/llm-extract/base/output.mdx";
import ExtractNoSchemaCURL from "/snippets/fr/v1/llm-extract/no-schema/curl.mdx";
import ExtractNoSchemaOutput from "/snippets/fr/v1/llm-extract/no-schema/output.mdx";

<div id="scrape-and-extract-structured-data-with-firecrawl">
  ## Extraire des données structurées avec Firecrawl
</div>

{/* <Warning>Scrape LLM Extract sera supprimé dans de futures versions. Veuillez utiliser le nouveau point de terminaison [Extract](/features/extract).</Warning> */}

Firecrawl utilise l’IA pour obtenir des données structurées à partir de pages web en 3 étapes :

1. **Définir le schéma :**
   Indiquez les données souhaitées en définissant un schéma JSON (au format OpenAI) ainsi que l’URL de la page.

2. **Envoyer la requête :**
   Transmettez votre URL et votre schéma à notre point de terminaison /scrape. Voir comment faire :
   [Documentation du point de terminaison /scrape](https://docs.firecrawl.dev/api-reference/endpoint/scrape)

3. **Récupérer vos données :**
   Recevez des données propres et structurées correspondant à votre schéma, prêtes à l’emploi.

Ainsi, l’obtention de données web au format dont vous avez besoin devient rapide et simple.

<div id="extract-structured-data">
  ## Extraire des données structurées
</div>

<div id="scrape-with-json-endpoint">
  ### Point de terminaison /scrape (avec json)
</div>

Permet d’extraire des données structurées à partir de pages scrappées.

<CodeGroup>

<ExtractPython />
<ExtractNode />
<ExtractCURL />

</CodeGroup>

Résultat :

<ExtractOutput />

<div id="extracting-without-schema-new">
  ### Extraction sans schéma (Nouveau)
</div>

Vous pouvez désormais extraire sans schéma en transmettant simplement un `prompt` au point de terminaison. Le LLM choisit la structure des données.

<CodeGroup>

<ExtractNoSchemaCURL />

</CodeGroup>

Résultat :

<ExtractNoSchemaOutput />

<div id="json-options-object">
  ### Objet d’options JSON
</div>

L’objet `jsonOptions` accepte les paramètres suivants :

- `schema` : Le schéma à utiliser pour l’extraction.
- `systemPrompt` : L’invite système à utiliser pour l’extraction.
- `prompt` : L’invite à utiliser pour l’extraction sans schéma.