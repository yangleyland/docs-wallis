---
title: "Extraction"
description: "Extraire des données structurées à partir de pages à l'aide de LLMs"
og:title: "Extraction | Firecrawl"
og:description: "Extraire des données structurées à partir de pages à l'aide de LLMs"
icon: "barcode-read"
sidebarTitle: "Extraction"
---

import ExtractCURL from "/snippets/fr/v1/extract/base/curl.mdx";
import ExtractPython from "/snippets/fr/v1/extract/base/python.mdx";
import ExtractNode from "/snippets/fr/v1/extract/base/js.mdx";
import ExtractOutput from "/snippets/fr/v1/extract/base/output.mdx";
import ExtractNoSchemaCURL from "/snippets/fr/v1/extract/no-schema/curl.mdx";
import ExtractNoSchemaPython from "/snippets/fr/v1/extract/no-schema/python.mdx";
import ExtractNoSchemaJS from "/snippets/fr/v1/extract/no-schema/js.mdx";
import ExtractNoSchemaOutput from "/snippets/fr/v1/extract/no-schema/output.mdx";
import ExtractWebSearchPython from "/snippets/fr/v1/extract/websearch/python.mdx";
import ExtractWebSearchJS from "/snippets/fr/v1/extract/websearch/js.mdx";
import ExtractWebSearchCURL from "/snippets/fr/v1/extract/websearch/curl.mdx";
import ExtractWebSearchOutput from "/snippets/fr/v1/extract/websearch/output.mdx";
import CheckExtractJobCURL from "/snippets/fr/v1/extract/status/curl.mdx";
import CheckExtractJobJS from "/snippets/fr/v1/extract/status/js.mdx";
import CheckExtractJobPython from "/snippets/fr/v1/extract/status/python.mdx";
import ExtractStatusPending from "/snippets/fr/v1/extract/status/pending.mdx";
import ExtractStatusAsync from "/snippets/fr/v1/extract/async-response/async.mdx";
import ExtractStatusDone from "/snippets/fr/v1/extract/status/completed.mdx";
import ExtractWithoutURLsPython from "/snippets/fr/v1/extract/without-urls/python.mdx";
import ExtractWithoutURLsJS from "/snippets/fr/v1/extract/without-urls/js.mdx";
import ExtractWithoutURLsCURL from "/snippets/fr/v1/extract/base/curl.mdx";

Le point de terminaison `/extract` simplifie la collecte de données structurées à partir d’un nombre quelconque d’URL ou de domaines entiers. Fournissez une liste d’URL, éventuellement avec des caractères génériques (par exemple `example.com/*`), ainsi qu’un prompt ou un schéma décrivant les informations souhaitées. Firecrawl se charge de l’exploration, de l’analyse et de la consolidation de jeux de données, grands ou petits.

<Info>Nous avons simplifié la facturation afin que Extract utilise désormais des crédits, comme tous les autres points de terminaison. Chaque crédit équivaut à 15 tokens.</Info>

<div id="using-extract">
  ## Utilisation de `/extract`
</div>

Vous pouvez extraire des données structurées depuis une ou plusieurs URL, y compris avec des caractères génériques :

* **Page unique**
  Exemple : `https://firecrawl.dev/some-page`
* **Plusieurs pages / Domaine complet**
  Exemple : `https://firecrawl.dev/*`

Lorsque vous utilisez `/*`, Firecrawl lance automatiquement un crawl et analyse toutes les URL qu&#39;il peut découvrir sur ce domaine, puis extrait les données demandées. Cette fonctionnalité est expérimentale ; envoyez un e-mail à [help@firecrawl.com](mailto:help@firecrawl.com) si vous rencontrez des problèmes.

<div id="example-usage">
  ### Exemple d’utilisation
</div>

<CodeGroup>

<ExtractPython />
<ExtractNode />
<ExtractCURL />

</CodeGroup>

**Paramètres clés :**

- **urls** : Un tableau d’une ou plusieurs URL. Prend en charge les jokers (`/*`) pour un crawl plus large.
- **prompt** (Optionnel, sauf si aucun schéma) : Une instruction en langage naturel décrivant les données souhaitées ou la manière dont vous souhaitez qu’elles soient structurées.
- **schema** (Optionnel, sauf si aucun prompt) : Une structure plus stricte si vous connaissez déjà le format JSON.
- **enableWebSearch** (Optionnel) : Lorsque `true`, l’extraction peut suivre des liens en dehors du domaine spécifié.

Voir la [référence de l’API](https://docs.firecrawl.dev/api-reference/endpoint/extract) pour plus de détails.

<div id="response-sdks">
  ### Réponse (SDKs)
</div>

<ExtractOutput />

<div id="asynchronous-extraction-status-checking">
  ## Extraction asynchrone et vérification de l’état
</div>

Lorsque vous soumettez un travail d’extraction — directement via l’API ou via les méthodes asynchrones du SDK — vous recevez un ID de travail. Vous pouvez utiliser cet ID pour :

- Vérifier l’état du travail : Envoyez une requête au point de terminaison /extract/{ID} pour voir si le travail est toujours en cours ou terminé.
- Interrogation automatique (comportement par défaut du SDK) : Si vous utilisez la méthode extract par défaut (Python/Node), le SDK interroge automatiquement ce point de terminaison pour vous et renvoie les résultats finaux une fois le travail terminé.
- Interrogation manuelle (méthodes asynchrones du SDK) : Si vous utilisez les méthodes asynchrones — async_extract (Python) ou asyncExtract (Node) — le SDK renvoie immédiatement un ID de travail que vous pouvez suivre. Utilisez get_extract_status (Python) ou getExtractStatus (Node) pour vérifier l’avancement du travail selon votre propre cadence.

<Note>
  Ce point de terminaison ne fonctionne que pour les travaux en cours ou récemment terminés (dans les 24
  heures).
</Note>

Ci-dessous, des exemples de code pour vérifier l’état d’un travail d’extraction avec Python, Node.js et cURL :

<CodeGroup>

<CheckExtractJobPython />
<CheckExtractJobJS />
<CheckExtractJobCURL />

</CodeGroup>

<div id="possible-states">
  ### États possibles
</div>

- **completed**: Extraction terminée avec succès.
- **processing**: Firecrawl traite encore votre demande.
- **failed**: Une erreur est survenue ; les données n'ont pas été entièrement extraites.
- **cancelled**: La tâche a été annulée par l'utilisateur.

<div id="pending-example">
  #### Exemple en cours
</div>

<ExtractStatusPending />

<div id="completed-example">
  #### Exemple terminé
</div>

<ExtractStatusDone />

<div id="extracting-without-a-schema">
  ## Extraction sans schéma
</div>

Si vous préférez éviter de définir une structure stricte, vous pouvez simplement fournir un `prompt`. Le modèle sous-jacent choisira une structure pour vous, ce qui peut être utile pour des demandes plus exploratoires ou flexibles.

<CodeGroup>

<ExtractNoSchemaPython />
<ExtractNoSchemaJS />
<ExtractNoSchemaCURL />

</CodeGroup>

<ExtractNoSchemaOutput />

<div id="improving-results-with-web-search">
  ## Améliorer les résultats avec la recherche web
</div>

Définir `enableWebSearch = true` dans votre requête étendra l’exploration au-delà de l’ensemble d’URL fourni. Cela permet de récupérer des informations complémentaires ou liées à partir des pages référencées.

Voici un exemple qui extrait des informations sur les caméras embarquées, en enrichissant les résultats avec des données provenant de pages connexes :

<CodeGroup>

<ExtractWebSearchPython />
<ExtractWebSearchJS />
<ExtractWebSearchCURL />

</CodeGroup>

<div id="example-response-with-web-search">
  ### Exemple de réponse avec recherche sur le web
</div>

<ExtractWebSearchOutput />

La réponse inclut un contexte supplémentaire obtenu à partir de pages connexes, offrant des informations plus complètes et plus précises.

<div id="extracting-without-urls">
  ## Extraction sans URL
</div>

Le point de terminaison `/extract` prend désormais en charge l’extraction de données structurées à l’aide d’un prompt, sans avoir besoin d’URL précises. Utile pour la recherche ou lorsque les URL exactes sont inconnues. Actuellement en alpha.

<CodeGroup>

<ExtractWithoutURLsPython />
<ExtractWithoutURLsJS />
<ExtractWithoutURLsCURL />

</CodeGroup>

<div id="known-limitations-beta">
  ## Limites connues (bêta)
</div>

1. **Couverture de sites à grande échelle**  
   La couverture complète de sites massifs (p. ex. « tous les produits d’Amazon ») en une seule requête n’est pas encore prise en charge.

2. **Requêtes logiques complexes**  
   Des requêtes comme « trouver tous les articles de 2025 » peuvent ne pas renvoyer de façon fiable toutes les données attendues. Des capacités de requête plus avancées sont en cours de développement.

3. **Incohérences occasionnelles**  
   Les résultats peuvent varier d’une exécution à l’autre, en particulier pour les sites très vastes ou dynamiques. Les informations essentielles sont généralement capturées, mais des variations sont possibles.

4. **État bêta**  
   Comme `/extract` est encore en bêta, les fonctionnalités et les performances continueront d’évoluer. Nous accueillons vos signalements de bugs et vos retours pour nous aider à nous améliorer.

<div id="using-fire-1">
  ## Utilisation de FIRE-1
</div>

FIRE-1 est un agent IA qui renforce les capacités de scraping de Firecrawl. Il peut contrôler des actions du navigateur et naviguer dans des structures de sites web complexes afin de permettre une extraction de données exhaustive, au-delà des méthodes de scraping traditionnelles.

Vous pouvez exploiter l’agent FIRE-1 avec le point de terminaison `/v1/extract` pour des tâches d’extraction complexes nécessitant une navigation sur plusieurs pages ou une interaction avec des éléments.

**Exemple (cURL) :**

```bash
curl -X POST https://api.firecrawl.dev/v1/extract \
    -H 'Content-Type: application/json' \
    -H 'Authorization: Bearer VOTRE_JETON_API' \
    -d '{
      "urls": ["https://example-forum.com/topic/123"],
      "prompt": "Extrait tous les commentaires des utilisateurs de cette discussion de forum.",
      "schema": {
        "type": "object",
        "properties": {
          "comments": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "author": {"type": "string"},
                "comment_text": {"type": "string"}
              },
              "required": ["author", "comment_text"]
            }
          }
        },
        "required": ["comments"]
      },
      "agent": {
        "model": "FIRE-1"
      }
    }'
```

> FIRE-1 est déjà disponible et accessible en aperçu.

<div id="billing-and-usage-tracking">
  ## Facturation et suivi de l’utilisation
</div>

Nous avons simplifié la facturation : Extract utilise désormais des crédits, comme tous les autres endpoints. Chaque crédit correspond à 15 tokens.

Vous pouvez suivre l’utilisation d’Extract via le [tableau de bord](https://www.firecrawl.dev/app/extract).

Des commentaires ou besoin d’aide ? Écrivez à [help@firecrawl.com](mailto:help@firecrawl.com).