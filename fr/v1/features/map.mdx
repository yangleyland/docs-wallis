---
title: 'Map'
description: 'Saisissez un site web et obtenez toutes les URL du site — extrêmement rapidement'
og:title: "Map | Firecrawl"
og:description: "Passez d’une seule URL à une cartographie de l’ensemble du site web"
icon: "map"
version: "v1"
sidebarTitle: "Map"
---

import InstallationPython from "/snippets/fr/v1/installation/python.mdx";
import InstallationJavaScript from "/snippets/fr/v1/installation/js.mdx";
import InstallationGo from "/snippets/fr/v1/installation/go.mdx";
import InstallationRust from "/snippets/fr/v1/installation/rust.mdx";
import MapPython from "/snippets/fr/v1/map/base/python.mdx";
import MapJavaScript from "/snippets/fr/v1/map/base/js.mdx";
import MapGo from "/snippets/fr/v1/map/base/go.mdx";
import MapRust from "/snippets/fr/v1/map/base/rust.mdx";
import MapCURL from "/snippets/fr/v1/map/base/curl.mdx";
import MapResponse from "/snippets/fr/v1/map/base/output.mdx";

<div id="introducing-map">
  ## Présentation de /map
</div>

La façon la plus simple de passer d’une seule URL à une cartographie de l’ensemble du site. C’est extrêmement utile pour :

* Lorsque vous devez demander à l’utilisateur final de choisir quels liens scraper
* Quand vous avez besoin d’identifier rapidement les liens d’un site
* Quand vous devez scraper des pages d’un site liées à un sujet précis (utilisez le paramètre `search`)
* Quand vous ne devez scraper que des pages spécifiques d’un site

<div id="mapping">
  ## Mapping
</div>

<div id="map-endpoint">
  ### point de terminaison /map
</div>

Permet de cartographier une URL et d’obtenir les URL du site. Renvoie la plupart des liens présents sur le site.

<div id="installation">
  ### Installation
</div>

<CodeGroup>
  <InstallationPython />

  <InstallationJavaScript />

  <InstallationGo />

  <InstallationRust />
</CodeGroup>

<div id="usage">
  ### Utilisation
</div>

<CodeGroup>
  <MapPython />

  <MapJavaScript />

  <MapGo />

  <MapRust />

  <MapCURL />
</CodeGroup>

<div id="response">
  ### Réponse
</div>

Les SDK renvoient directement l’objet de données. cURL renvoie la charge utile exactement comme ci-dessous.

<MapResponse />

<div id="map-with-search">
  #### Cartographier avec recherche
</div>

La cartographie avec le paramètre `search` vous permet de rechercher des URL spécifiques au sein d’un site web.

```bash cURL
curl -X POST https://api.firecrawl.dev/v1/map \
    -H 'Content-Type: application/json' \
    -H 'Authorization: Bearer VOTRE_CLÉ_API' \
    -d '{
      "url": "https://firecrawl.dev",
      "search": "docs"
    }'
```

La réponse sera une liste ordonnée, de la plus pertinente à la moins pertinente.

```json
{
  "status": "réussi",
  "links": [
    "https://docs.firecrawl.dev",
    "https://docs.firecrawl.dev/sdks/python",
    "https://docs.firecrawl.dev/learn/rag-llama3",
  ]
}
```

<div id="considerations">
  ## Considérations
</div>

Ce point de terminaison privilégie la vitesse, il se peut donc qu’il ne capture pas tous les liens du site. Il s’appuie principalement sur le sitemap du site, complété par des données de crawl mises en cache et les résultats des moteurs de recherche. Pour une liste d’URL plus exhaustive et à jour, envisagez plutôt d’utiliser le point de terminaison [/crawl](/fr/features/crawl).