---
title: 'Rust'
description: 'Firecrawl Rust SDK は、ウェブサイトのスクレイピングやクロールを簡単に行い、言語モデル（LLM）でそのまま使えるフォーマットでデータを出力するためのライブラリです。'
icon: 'rust'
og:title: "Rust SDK | Firecrawl"
og:description: "Firecrawl Rust SDK は、ウェブサイトのスクレイピングやクロールを簡単に行い、言語モデル（LLM）でそのまま使えるフォーマットでデータを出力するためのライブラリです。"
---

import InstallationRust from '/snippets/ja/v1/installation/rust.mdx'
import ScrapeAndCrawlRustExample from '/snippets/ja/v1/scrape-and-crawl/rust.mdx'
import ScrapeRustShort from '/snippets/ja/v1/scrape/short/rust.mdx'
import CrawlRustShort from '/snippets/ja/v1/crawl/short/rust.mdx'
import CrawlAsyncRustShort from '/snippets/ja/v1/crawl-async/short/rust.mdx'
import MapRustShort from '/snippets/ja/v1/map/short/rust.mdx'
import LLMExtractRust from '/snippets/ja/v1/llm-extract/base/rust.mdx'

<Warning>
  このSDKは現在、最新ではないFirecrawl APIの**v1**を使用しています（v2が利用可能）。一部の機能や改善はv2でのみ利用できる場合があります。
</Warning>

<div id="installation">
  ## インストール
</div>

Firecrawl の Rust SDK をインストールするには、`Cargo.toml` に次を追加します。

<InstallationRust />

<div id="usage">
  ## 使い方
</div>

まずは [firecrawl.dev](https://firecrawl.dev) で API キーを取得します。次に `FirecrawlApp` を初期化します。以降は `FirecrawlApp::scrape_url` などの関数を利用して、Firecrawl の API を呼び出せます。

以下は Rust での SDK 利用例です:

<ScrapeAndCrawlRustExample />

<div id="scraping-a-url">
  ### URLをスクレイピングする
</div>

単一のURLをスクレイピングするには、`scrape_url` メソッドを使用します。URLを引数に取り、スクレイプしたデータを `Document` として返します。

<ScrapeRustShort />

<div id="scraping-with-extract">
  ### Extract を使ったスクレイピング
</div>

Extract を使うと、任意の URL から構造化データを簡単に抽出できます。スキーマは `serde_json::json!` マクロを使い、JSON Schema 形式で指定します。

<LLMExtractRust />

<div id="crawling-a-website">
  ### ウェブサイトのクロール
</div>

ウェブサイトをクロールするには、`crawl_url` メソッドを使用します。クロールが完了するまで待機し、開始URLや指定したオプションによっては時間がかかる場合があります。

<CrawlRustShort />

<div id="crawling-asynchronously">
  #### 非同期でクロールする
</div>

結果を待たずにクロールするには、`crawl_url_async` メソッドを使用します。引数は同じですが、クロールの ID を含む `CrawlAsyncRespone` 構造体を返します。その ID を `check_crawl_status` メソッドで使用すると、いつでもステータスを確認できます。完了したクロールは 24 時間後に削除されます。

<CrawlAsyncRustShort />

<div id="map-a-url">
  ### URL をマッピング
</div>

開始URLから関連リンクをすべてマッピングします。

<MapRustShort />

<div id="error-handling">
  ## エラーハンドリング
</div>

SDK は、Firecrawl API や依存ライブラリから返されるエラーを受け取り、`Error`、`Debug`、`Display` を実装する `FirecrawlError` 列挙型に集約します。すべてのメソッドは `Result<T, FirecrawlError>` を返します。