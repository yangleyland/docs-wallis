---
title: "検索"
description: "ウェブを検索し、結果からページ全体のコンテンツを取得"
og:title: "検索 | Firecrawl"
og:description: "ウェブを検索し、結果からページ全体のコンテンツを取得"
icon: "magnifying-glass"
---

import InstallationPython from "/snippets/ja/v1/installation/python.mdx";
import InstallationNode from "/snippets/ja/v1/installation/js.mdx";
import InstallationGo from "/snippets/ja/v1/installation/go.mdx";
import InstallationRust from "/snippets/ja/v1/installation/rust.mdx";
import SearchPython from "/snippets/ja/v1/search/base/python.mdx";
import SearchNode from "/snippets/ja/v1/search/base/js.mdx";
import SearchCURL from "/snippets/ja/v1/search/base/curl.mdx";
import SearchContentPython from "/snippets/ja/v1/search/content/python.mdx";
import SearchContentNode from "/snippets/ja/v1/search/content/js.mdx";
import SearchContentCURL from "/snippets/ja/v1/search/content/curl.mdx";
import SearchLocationPython from "/snippets/ja/v1/search/location/python.mdx";
import SearchLocationNode from "/snippets/ja/v1/search/location/js.mdx";
import SearchLocationCURL from "/snippets/ja/v1/search/location/curl.mdx";
import SearchTimePython from "/snippets/ja/v1/search/time/python.mdx";
import SearchTimeNode from "/snippets/ja/v1/search/time/js.mdx";
import SearchTimeCURL from "/snippets/ja/v1/search/time/curl.mdx";

Firecrawlの検索APIを使うと、ウェブ検索を実行し、必要に応じて検索結果を同一の操作でスクレイピングできます。

* 出力フォーマットを選択（markdown、HTML、links、screenshots）
* 位置情報などのパラメータを指定してウェブ検索
* 検索結果のページ内容を、必要に応じて各種フォーマットで取得
* 取得件数の制御やタイムアウトの設定が可能

詳細は、[Search Endpoint API Reference](https://docs.firecrawl.dev/api-reference/endpoint/search)を参照してください。

<div id="performing-a-search-with-firecrawl">
  ## Firecrawl で検索を行う
</div>

<div id="search-endpoint">
  ### /search エンドポイント
</div>

ウェブ検索を実行し、必要に応じて結果ページからコンテンツを取得するために使用します。

<div id="installation">
  ### インストール
</div>

<CodeGroup>

  <InstallationPython />

  <InstallationNode />

  <InstallationGo />
  
  <InstallationRust />

</CodeGroup>

<div id="basic-usage">
  ### 基本的な使用方法
</div>

<CodeGroup>

  <SearchPython />

  <SearchNode />
  
  <SearchCURL />

</CodeGroup>

<div id="response">
  ### レスポンス
</div>

各種SDKはデータオブジェクトを直接返します。cURLは完全なペイロードを返します。

```json
{
  "success": true,
  "data": [
    {
      "title": "Firecrawl - 究極のWebスクレイピングAPI",
      "description": "Firecrawlは、あらゆるWebサイトをAIや分析向けのクリーンで構造化されたデータへ変換する強力なWebスクレイピングAPIです。",
      "url": "https://firecrawl.dev/"
    },
    {
      "title": "FirecrawlによるWebスクレイピング完全ガイド",
      "description": "Firecrawlを使ってWebサイトから手間なくデータを抽出する方法を解説します。",
      "url": "https://firecrawl.dev/guides/web-scraping/"
    },
    {
      "title": "Firecrawlドキュメント - はじめに",
      "description": "Firecrawl WebスクレイピングAPIの公式ドキュメント。",
      "url": "https://docs.firecrawl.dev/"
    }
    // ... さらに結果があります
  ]
}
```

<div id="search-with-content-scraping">
  ## コンテンツスクレイピング付き検索
</div>

検索と検索結果からのコンテンツ取得を、1回の操作で実行します。

<CodeGroup>

  <SearchContentPython />

  <SearchContentNode />
  
  <SearchContentCURL />

</CodeGroup>

<div id="response-with-scraped-content">
  ### スクレイプしたコンテンツを含むレスポンス
</div>

```json
{
  "success": true,
  "data": [
    {
      "title": "Firecrawl - 最高峰のWebスクレイピングAPI",
      "description": "Firecrawlは、あらゆるWebサイトをAIや分析に使えるクリーンで構造化されたデータへと変換する強力なWebスクレイピングAPIです。",
      "url": "https://firecrawl.dev/",
      "markdown": "# Firecrawl\n\n最高峰のWebスクレイピングAPI\n\n## あらゆるWebサイトをクリーンで構造化されたデータに変換\n\nFirecrawlは、AIアプリケーション、マーケットリサーチ、コンテンツ集約などに向けて、Webサイトからのデータ抽出を容易にします...",
      "links": [
        "https://firecrawl.dev/pricing",
        "https://firecrawl.dev/docs",
        "https://firecrawl.dev/guides",
        // ... さらにリンク
      ],
      "metadata": {
        "title": "Firecrawl - 最高峰のWebスクレイピングAPI",
        "description": "Firecrawlは、あらゆるWebサイトをAIや分析に使えるクリーンで構造化されたデータへと変換する強力なWebスクレイピングAPIです。",
        "sourceURL": "https://firecrawl.dev/",
        "statusCode": 200
      }
    },
    // ... さらに結果
  ]
}
```

<div id="advanced-search-options">
  ## 高度な検索オプション
</div>

Firecrawl の検索APIは、検索をカスタマイズできるさまざまなパラメータに対応しています。

<div id="location-customization">
  ### ロケーションのカスタマイズ
</div>

<CodeGroup>

  <SearchLocationPython />

  <SearchLocationNode />
  
  <SearchLocationCURL />

</CodeGroup>

<div id="time-based-search">
  ### 時間ベースの検索
</div>

`tbs` パラメータを使って、結果を期間でフィルタリングします。

<CodeGroup>
  <SearchTimePython />

  <SearchTimeNode />

  <SearchTimeCURL />
</CodeGroup>

一般的な `tbs` の値:

* `qdr:h` - 過去1時間
* `qdr:d` - 過去24時間
* `qdr:w` - 過去1週間
* `qdr:m` - 過去1か月
* `qdr:y` - 過去1年

より細かな期間指定が必要な場合は、カスタム日付範囲のフォーマットで厳密な日付範囲を指定できます。

<CodeGroup>
  ```python Python
  from firecrawl import Firecrawl

  # APIキーでクライアントを初期化
  app = Firecrawl(api_key="fc-YOUR_API_KEY")

  # 2024年12月の結果を検索
  search_result = app.search(
      "firecrawl updates",
      limit=10,
      tbs="cdr:1,cd_min:12/1/2024,cd_max:12/31/2024"
  )
  ```

  ```js JavaScript
  import { Firecrawl } from 'firecrawl';

  // APIキーでクライアントを初期化
  const app = new Firecrawl({apiKey: "fc-YOUR_API_KEY"});

  // 2024年12月の結果を検索
  app.search("firecrawl updates", {
    limit: 10,
    tbs: "cdr:1,cd_min:12/1/2024,cd_max:12/31/2024"
  })
  .then(searchResult => {
    console.log(searchResult.data);
  });
  ```

  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v1/search \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer fc-YOUR_API_KEY" \
    -d '{
      "query": "firecrawl updates",
      "limit": 10,
      "tbs": "cdr:1,cd_min:12/1/2024,cd_max:12/31/2024"
    }'
  ```
</CodeGroup>

<div id="custom-timeout">
  ### カスタムタイムアウト
</div>

検索処理にカスタムのタイムアウトを設定します:

<CodeGroup>
  ```python Python
  from firecrawl import Firecrawl

  # APIキーでクライアントを初期化
  app = Firecrawl(api_key="fc-YOUR_API_KEY")

  # タイムアウトを30秒に設定
  search_result = app.search(
      "complex search query",
      limit=10,
      timeout=30000  # 30秒（ミリ秒）
  )
  ```

  ```js JavaScript
  import { Firecrawl } from 'firecrawl';

  // APIキーでクライアントを初期化
  const app = new Firecrawl({apiKey: "fc-YOUR_API_KEY"});

  // タイムアウトを30秒に設定
  app.search("complex search query", {
    limit: 10,
    timeout: 30000  // 30秒（ミリ秒）
  })
  .then(searchResult => {
    // 結果を処理
    console.log(searchResult.data);
  });
  ```

  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v1/search \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer fc-YOUR_API_KEY" \
    -d '{
      "query": "complex search query",
      "limit": 10,
      "timeout": 30000
    }'
  ```
</CodeGroup>

<div id="scraping-options">
  ## スクレイピングオプション
</div>

検索結果をスクレイピングする際は、複数の出力フォーマットや高度なスクレイピング設定を指定できます。

<CodeGroup>
  ```python Python
  from firecrawl import Firecrawl, ScrapeOptions

  # APIキーでクライアントを初期化
  app = Firecrawl(api_key="fc-YOUR_API_KEY")

  # 複数フォーマットで検索結果を取得
  search_result = app.search(
      "firecrawl features",
      limit=3,
      scrape_options=ScrapeOptions(formats=["markdown", "html", "links", "screenshot"])
  )
  ```

  ```js JavaScript
  import { Firecrawl } from 'firecrawl';

  // APIキーでクライアントを初期化
  const app = new Firecrawl({apiKey: "fc-YOUR_API_KEY"});

  // 複数フォーマットで検索結果を取得
  app.search("firecrawl features", {
    limit: 3,
    scrapeOptions: {
      formats: ["markdown", "html", "links", "screenshot"]
    }
  })
  .then(searchResult => {
    // 各フォーマットの結果を処理
    console.log(searchResult.data);
  });
  ```

  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v1/search \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer fc-YOUR_API_KEY" \
    -d '{
      "query": "firecrawl features",
      "limit": 3,
      "scrapeOptions": {
        "formats": ["markdown", "html", "links", "screenshot"]
      }
    }'
  ```
</CodeGroup>

利用可能なフォーマット:

* `markdown`: クリーンに整形されたMarkdownコンテンツ
* `html`: 処理済みのHTMLコンテンツ
* `rawHtml`: 未加工のHTMLコンテンツ
* `links`: ページ内で検出されたリンク一覧
* `screenshot`: ページのスクリーンショット
* `screenshot@fullPage`: ページ全体のスクリーンショット
* `extract`: 構造化データの抽出

<div id="cost-implications">
  ## コストへの影響
</div>

検索のコストは、検索結果10件あたり2クレジットです。スクレイピングオプションを有効にしている場合、各検索結果に対して通常のスクレイピングコストが適用されます:

- **Basic scrape**: Webページ1件あたり1クレジット
- **PDF parsing**: PDFページ1枚あたり1クレジット
- **Enhanced proxy mode**: Webページ1件あたり追加で4クレジット
- **JSON mode**: Webページ1件あたり追加で4クレジット

コストを抑えるには、次のように設定します:

- PDF解析が不要な場合は `parsers: []` を設定する
- 可能な場合は `"enhanced"` の代わりに `proxy: "basic"` を使用するか、`"auto"` に設定する
- `limit` パラメータで検索結果の件数を制限する

<div id="advanced-scraping-options">
  ## 高度なスクレイピングオプション
</div>

スクレイピングオプションの詳細は、[Scrape 機能のドキュメント](https://docs.firecrawl.dev/features/scrape)を参照してください。FIRE-1（エージェント）とchangeTracking 機能を除き、その他はすべてこの /search エンドポイントでサポートされています。