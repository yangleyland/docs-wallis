---
title: 'Map'
description: 'ウェブサイトを入力すると、そのサイト内のすべてのURLを超高速で取得できます'
og:title: "Map | Firecrawl"
og:description: "単一のURLからウェブサイト全体のマップへ"
icon: "map"
version: "v1"
sidebarTitle: "Map"
---

import InstallationPython from "/snippets/ja/v1/installation/python.mdx";
import InstallationJavaScript from "/snippets/ja/v1/installation/js.mdx";
import InstallationGo from "/snippets/ja/v1/installation/go.mdx";
import InstallationRust from "/snippets/ja/v1/installation/rust.mdx";
import MapPython from "/snippets/ja/v1/map/base/python.mdx";
import MapJavaScript from "/snippets/ja/v1/map/base/js.mdx";
import MapGo from "/snippets/ja/v1/map/base/go.mdx";
import MapRust from "/snippets/ja/v1/map/base/rust.mdx";
import MapCURL from "/snippets/ja/v1/map/base/curl.mdx";
import MapResponse from "/snippets/ja/v1/map/base/output.mdx";

<div id="introducing-map">
  ## /map の紹介
</div>

単一のURLからウェブサイト全体のマップを素早く取得する最も簡単な方法です。次の用途に非常に有用です:

* エンドユーザーにスクレイプ対象のリンクを選ばせたいとき
* サイト上のリンクをすばやく把握したいとき
* 特定のトピックに関連するページをスクレイプしたいとき (`search` パラメータを使用) 
* サイトの特定ページだけをスクレイプしたいとき

<div id="mapping">
  ## マッピング
</div>

<div id="map-endpoint">
  ### /map エンドポイント
</div>

URL をマッピングし、サイトに関連する URL を取得するために使用します。ウェブサイト上に存在するリンクの大部分を返します。

<div id="installation">
  ### インストール
</div>

<CodeGroup>
  <InstallationPython />

  <InstallationJavaScript />

  <InstallationGo />

  <InstallationRust />
</CodeGroup>

<div id="usage">
  ### 使用方法
</div>

<CodeGroup>
  <MapPython />

  <MapJavaScript />

  <MapGo />

  <MapRust />

  <MapCURL />
</CodeGroup>

<div id="response">
  ### レスポンス
</div>

SDK はデータオブジェクトを直接返します。cURL は下記のとおり、ペイロードをそのまま返します。

<MapResponse />

<div id="map-with-search">
  #### 検索付きマップ
</div>

`search` パラメータを使うと、サイト内の特定のURLを検索できます。

```bash cURL
curl -X POST https://api.firecrawl.dev/v1/map \
    -H 'Content-Type: application/json' \
    -H 'Authorization: Bearer YOUR_API_KEY' \
    -d '{
      "url": "https://firecrawl.dev",
      "search": "docs"
    }'
```

回答は、関連性の高い順に並んだ番号付きリストで返されます。

```json
{
  "status": "success",
  "links": [
    "https://docs.firecrawl.dev",
    "https://docs.firecrawl.dev/sdks/python",
    "https://docs.firecrawl.dev/learn/rag-llama3",
  ]
}
```

<div id="considerations">
  ## 注意事項
</div>

このエンドポイントは速度を優先しているため、サイト内のリンクをすべて取得できない場合があります。主にウェブサイトのサイトマップを参照し、キャッシュされたクロールデータと検索エンジンの結果で補完しています。より網羅的かつ最新の URL リストが必要な場合は、代わりに [/crawl](/ja/features/crawl) エンドポイントの利用を検討してください。