---
title: "生成 LLMs.txt"
openapi: "/zh/api-reference/v1-openapi.json POST /llmstxt"
---

LLMs.txt 生成端点可为任意网站创建 LLMs.txt 和 LLMs-full.txt 文件。这些文件以结构化、对 LLM 友好的格式呈现网站内容，便于语言模型理解和处理信息。

在找状态端点？请查看 [LLMs.txt 状态](/zh/api-reference/endpoint/llmstxt-get) 端点。

<div id="response-structure">
  ### 响应结构
</div>

响应包括：

* **success**：表示请求是否成功的布尔值
* **id**：生成任务的唯一标识符

<div id="output-format">
  ### 输出格式
</div>

1. **LLMs.txt**

   * 网站的精炼、结构化摘要
   * 包含关键链接与说明
   * 使用 Markdown 格式，便于解析
   * 示例：

     ```markdown
     # http://example.com llms.txt

     - [页面标题](https://example.com/page)：简要说明
     - [另一页面](https://example.com/another)：另一段说明
     ```

2. **LLMs-full.txt**（当 showFullText 为 true 时）

   * 包含已处理页面的完整内容
   * 保持层级结构
   * 提供更详细的信息
   * 示例：

     ```markdown
     # http://example.com llms-full.txt

     ## 页面标题

     页面完整内容……

     ## 另一页面

     更详细的内容……
     ```

<div id="billing">
  ### 计费
</div>

计费基于 API 调用次数和处理的 URL 数量：

- 基础费用：每次 API 调用消耗 1 个 credit
- 额外费用：每处理 1 个 URL 消耗 1 个 credit
- 可通过 `maxUrls` 参数控制 URL 成本