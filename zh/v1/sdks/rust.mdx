---
title: 'Rust'
description: 'Firecrawl Rust SDK 是一个库，助你轻松抓取和爬取网站，并以适配大语言模型（LLM）的格式输出数据。'
icon: 'rust'
og:title: "Rust SDK | Firecrawl"
og:description: "Firecrawl Rust SDK 是一个库，助你轻松抓取和爬取网站，并以适配大语言模型（LLM）的格式输出数据。"
---

import InstallationRust from '/snippets/zh/v1/installation/rust.mdx'
import ScrapeAndCrawlRustExample from '/snippets/zh/v1/scrape-and-crawl/rust.mdx'
import ScrapeRustShort from '/snippets/zh/v1/scrape/short/rust.mdx'
import CrawlRustShort from '/snippets/zh/v1/crawl/short/rust.mdx'
import CrawlAsyncRustShort from '/snippets/zh/v1/crawl-async/short/rust.mdx'
import MapRustShort from '/snippets/zh/v1/map/short/rust.mdx'
import LLMExtractRust from '/snippets/zh/v1/llm-extract/base/rust.mdx'

<Warning>
  此 SDK 目前使用 Firecrawl API 的 **v1** 版本，并非最新（v2 已可用）。部分功能和改进仅在 v2 提供。
</Warning>

<div id="installation">
  ## 安装
</div>

要安装 Firecrawl 的 Rust SDK，请在你的 `Cargo.toml` 中添加以下内容：

<InstallationRust />

<div id="usage">
  ## 用法
</div>

首先，你需要在 [firecrawl.dev](https://firecrawl.dev) 获取 API 密钥。接着，初始化 `FirecrawlApp`。随后即可调用 `FirecrawlApp::scrape_url` 等函数来使用我们的 API。

以下是在 Rust 中使用该 SDK 的示例：

<ScrapeAndCrawlRustExample />

<div id="scraping-a-url">
  ### 抓取 URL
</div>

要抓取单个 URL，请使用 `scrape_url` 方法。该方法接收 URL 作为参数，并返回一个 `Document` 类型的抓取结果。

<ScrapeRustShort />

<div id="scraping-with-extract">
  ### 使用 Extract 进行抽取
</div>

借助 Extract，你可以轻松从任意 URL 提取结构化数据。你需要使用 `serde_json::json!` 宏，以 JSON Schema 格式定义你的 schema。

<LLMExtractRust />

<div id="crawling-a-website">
  ### 爬取网站
</div>

要爬取网站，请使用 `crawl_url` 方法。该方法会等待爬取过程完成，耗时长短取决于起始 URL 和所选配置，可能需要较长时间。

<CrawlRustShort />

<div id="crawling-asynchronously">
  #### 异步爬取
</div>

若无需等待结果，请使用 `crawl_url_async` 方法。它接受相同参数，但会返回一个包含爬取 ID 的 `CrawlAsyncRespone` 结构体。你可以使用该 ID 通过 `check_crawl_status` 方法随时查询状态。请注意，已完成的爬取会在 24 小时后删除。

<CrawlAsyncRustShort />

<div id="map-a-url">
  ### 映射 URL
</div>

从起始 URL 获取其所有关联链接的映射。

<MapRustShort />

<div id="error-handling">
  ## 错误处理
</div>

该 SDK 会处理由 Firecrawl API 和其依赖项返回的错误，并将它们统一为 `FirecrawlError` 枚举；该枚举实现了 `Error`、`Debug` 和 `Display`。我们所有方法均返回 `Result<T, FirecrawlError>`。