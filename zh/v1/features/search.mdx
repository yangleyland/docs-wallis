---
title: "搜索"
description: "在网上搜索并获取结果的完整内容"
og:title: "搜索 | Firecrawl"
og:description: "在网上搜索并获取结果页面的完整内容"
icon: "magnifying-glass"
---

import InstallationPython from "/snippets/zh/v1/installation/python.mdx";
import InstallationNode from "/snippets/zh/v1/installation/js.mdx";
import InstallationGo from "/snippets/zh/v1/installation/go.mdx";
import InstallationRust from "/snippets/zh/v1/installation/rust.mdx";
import SearchPython from "/snippets/zh/v1/search/base/python.mdx";
import SearchNode from "/snippets/zh/v1/search/base/js.mdx";
import SearchCURL from "/snippets/zh/v1/search/base/curl.mdx";
import SearchContentPython from "/snippets/zh/v1/search/content/python.mdx";
import SearchContentNode from "/snippets/zh/v1/search/content/js.mdx";
import SearchContentCURL from "/snippets/zh/v1/search/content/curl.mdx";
import SearchLocationPython from "/snippets/zh/v1/search/location/python.mdx";
import SearchLocationNode from "/snippets/zh/v1/search/location/js.mdx";
import SearchLocationCURL from "/snippets/zh/v1/search/location/curl.mdx";
import SearchTimePython from "/snippets/zh/v1/search/time/python.mdx";
import SearchTimeNode from "/snippets/zh/v1/search/time/js.mdx";
import SearchTimeCURL from "/snippets/zh/v1/search/time/curl.mdx";

Firecrawl 的搜索 API 允许你进行网页搜索，并可在一次操作中可选地抓取搜索结果。

* 选择特定输出 formats（markdown、HTML、links、screenshots）
* 使用可自定义参数（location 等）进行全网搜索
* 可选以多种 formats 从搜索结果中获取内容
* 控制结果数量并设置超时

详情请参见 [Search Endpoint API Reference](https://docs.firecrawl.dev/api-reference/endpoint/search)。

<div id="performing-a-search-with-firecrawl">
  ## 使用 Firecrawl 进行搜索
</div>

<div id="search-endpoint">
  ### /search 端点
</div>

用于执行网页搜索，并可选择性地从搜索结果中获取内容。

<div id="installation">
  ### 安装
</div>

<CodeGroup>

  <InstallationPython />

  <InstallationNode />

  <InstallationGo />
  
  <InstallationRust />

</CodeGroup>

<div id="basic-usage">
  ### 基本用法
</div>

<CodeGroup>

  <SearchPython />

  <SearchNode />
  
  <SearchCURL />

</CodeGroup>

<div id="response">
  ### 响应
</div>

SDK 会直接返回数据对象；cURL 会返回完整的请求载荷。

```json
{
  "success": true,
  "data": [
    {
      "title": "Firecrawl - 终极 Web 抓取 API",
      "description": "Firecrawl 是一款功能强大的 Web 抓取 API，可将任意网站转换为适用于 AI 与分析的干净、结构化数据。",
      "url": "https://firecrawl.dev/"
    },
    {
      "title": "使用 Firecrawl 进行 Web 抓取——完整指南",
      "description": "学习如何使用 Firecrawl 轻松从网站提取数据。",
      "url": "https://firecrawl.dev/guides/web-scraping/"
    },
    {
      "title": "Firecrawl 文档——快速上手",
      "description": "Firecrawl Web 抓取 API 的官方文档。",
      "url": "https://docs.firecrawl.dev/"
    }
    // ... 更多结果
  ]
}
```

<div id="search-with-content-scraping">
  ## 带内容抓取的搜索
</div>

一次操作即可搜索并提取搜索结果中的内容。

<CodeGroup>

  <SearchContentPython />

  <SearchContentNode />
  
  <SearchContentCURL />

</CodeGroup>

<div id="response-with-scraped-content">
  ### 包含抓取内容的响应
</div>

```json
{
  "success": true,
  "data": [
    {
      "title": "Firecrawl - 终极网页抓取 API",
      "description": "Firecrawl 是功能强大的网页抓取 API，可将任何网站转换为干净、结构化的数据，供 AI 与分析使用。",
      "url": "https://firecrawl.dev/",
      "markdown": "# Firecrawl\n\n终极网页抓取 API\n\n## 将任何网站转换为干净、结构化的数据\n\nFirecrawl 让网站数据提取变得简单，适用于 AI 应用、市场研究、内容聚合等……",
      "links": [
        "https://firecrawl.dev/pricing",
        "https://firecrawl.dev/docs",
        "https://firecrawl.dev/guides",
        // … 更多链接
      ],
      "metadata": {
        "title": "Firecrawl - 终极网页抓取 API",
        "description": "Firecrawl 是功能强大的网页抓取 API，可将任何网站转换为干净、结构化的数据，供 AI 与分析使用。",
        "sourceURL": "https://firecrawl.dev/",
        "statusCode": 200
      }
    },
    // … 更多结果
  ]
}
```

<div id="advanced-search-options">
  ## 高级搜索选项
</div>

Firecrawl 的搜索 API 支持通过多种参数来自定义搜索：

<div id="location-customization">
  ### 位置自定义设置
</div>

<CodeGroup>

  <SearchLocationPython />

  <SearchLocationNode />
  
  <SearchLocationCURL />

</CodeGroup>

<div id="time-based-search">
  ### 按时间搜索
</div>

使用 `tbs` 参数按时间筛选结果：

<CodeGroup>
  <SearchTimePython />

  <SearchTimeNode />

  <SearchTimeCURL />
</CodeGroup>

常用的 `tbs` 值：

* `qdr:h` - 过去 1 小时
* `qdr:d` - 过去 24 小时
* `qdr:w` - 过去 1 周
* `qdr:m` - 过去 1 个月
* `qdr:y` - 过去 1 年

如需更精确地按时间筛选，可使用自定义日期范围格式指定确切的日期范围：

<CodeGroup>
  ```python Python
  from firecrawl import Firecrawl

  # 使用你的 API 密钥初始化客户端
  app = Firecrawl(api_key="fc-YOUR_API_KEY")

  # 搜索 2024 年 12 月的结果
  search_result = app.search(
      "firecrawl updates",
      limit=10,
      tbs="cdr:1,cd_min:12/1/2024,cd_max:12/31/2024"
  )
  ```

  ```js JavaScript
  import { Firecrawl } from 'firecrawl';

  // 使用你的 API 密钥初始化客户端
  const app = new Firecrawl({apiKey: "fc-YOUR_API_KEY"});

  // 搜索 2024 年 12 月的结果
  app.search("firecrawl updates", {
    limit: 10,
    tbs: "cdr:1,cd_min:12/1/2024,cd_max:12/31/2024"
  })
  .then(searchResult => {
    console.log(searchResult.data);
  });
  ```

  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v1/search \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer fc-YOUR_API_KEY" \
    -d '{
      "query": "firecrawl updates",
      "limit": 10,
      "tbs": "cdr:1,cd_min:12/1/2024,cd_max:12/31/2024"
    }'
  ```
</CodeGroup>

<div id="custom-timeout">
  ### 自定义超时时间
</div>

为搜索操作设置自定义超时时间：

<CodeGroup>
  ```python Python
  from firecrawl import Firecrawl

  # 使用你的 API 密钥初始化客户端
  app = Firecrawl(api_key="fc-YOUR_API_KEY")

  # 将超时时间设置为 30 秒
  search_result = app.search(
      "complex search query",
      limit=10,
      timeout=30000  # 30 秒（毫秒）
  )
  ```

  ```js JavaScript
  import { Firecrawl } from 'firecrawl';

  // 使用你的 API 密钥初始化客户端
  const app = new Firecrawl({apiKey: "fc-YOUR_API_KEY"});

  // 将超时时间设置为 30 秒
  app.search("complex search query", {
    limit: 10,
    timeout: 30000  // 30 秒（毫秒）
  })
  .then(searchResult => {
    // 处理结果
    console.log(searchResult.data);
  });
  ```

  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v1/search \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer fc-YOUR_API_KEY" \
    -d '{
      "query": "complex search query",
      "limit": 10,
      "timeout": 30000
    }'
  ```
</CodeGroup>

<div id="scraping-options">
  ## 抓取选项
</div>

在抓取搜索结果时，你可以指定多个输出 formats 和高级抓取选项：

<CodeGroup>
  ```python Python
  from firecrawl import Firecrawl, ScrapeOptions

  # 使用你的 API 密钥初始化客户端
  app = Firecrawl(api_key="fc-YOUR_API_KEY")

  # 以多种 formats 获取搜索结果
  search_result = app.search(
      "firecrawl features",
      limit=3,
      scrape_options=ScrapeOptions(formats=["markdown", "html", "links", "screenshot"])
  )
  ```

  ```js JavaScript
  import { Firecrawl } from 'firecrawl';

  // 使用你的 API 密钥初始化客户端
  const app = new Firecrawl({apiKey: "fc-YOUR_API_KEY"});

  // 以多种 formats 获取搜索结果
  app.search("firecrawl features", {
    limit: 3,
    scrapeOptions: {
      formats: ["markdown", "html", "links", "screenshot"]
    }
  })
  .then(searchResult => {
    // 以多种 formats 处理结果
    console.log(searchResult.data);
  });
  ```

  ```bash cURL
  curl -X POST https://api.firecrawl.dev/v1/search \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer fc-YOUR_API_KEY" \
    -d '{
      "query": "firecrawl features",
      "limit": 3,
      "scrapeOptions": {
        "formats": ["markdown", "html", "links", "screenshot"]
      }
    }'
  ```
</CodeGroup>

可用 formats：

* `markdown`: 干净、格式良好的 Markdown 内容
* `html`: 处理后的 HTML 内容
* `rawHtml`: 未修改的 HTML 内容
* `links`: 页面中发现的链接列表
* `screenshot`: 页面截图
* `screenshot@fullPage`: 整页截图
* `extract`: 结构化数据提取

<div id="cost-implications">
  ## 成本说明
</div>

每次搜索的费用为每 10 条搜索结果消耗 2 个积分。如果启用了爬取选项，每个搜索结果将按标准爬取费用计费：

- **Basic scrape**：每个网页 1 个积分
- **PDF parsing**：每个 PDF 页面 1 个积分
- **Enhanced proxy mode**：每个网页额外 4 个积分
- **JSON mode**：每个网页额外 4 个积分

为帮助控制成本：

- 如果不需要 PDF 解析，将 `parsers` 设为 `[]`
- 在可能的情况下使用 `proxy: "basic"` 而不是 `"enhanced"`，或将其设置为 `"auto"`
- 使用 `limit` 参数限制搜索结果数量

<div id="advanced-scraping-options">
  ## 高级抓取选项
</div>

关于抓取选项的更多信息，请参阅 [Scrape 功能文档](https://docs.firecrawl.dev/features/scrape)。除 FIRE-1（代理）和 changeTracking 功能外，其余均受此 /search 端点支持。