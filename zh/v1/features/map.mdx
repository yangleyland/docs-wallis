---
title: 'Map'
description: '输入网站即可极速获取其全部 URL'
og:title: "Map | Firecrawl"
og:description: "从单个 URL 快速映射整个网站"
icon: "map"
version: "v1"
sidebarTitle: "Map"
---

import InstallationPython from "/snippets/zh/v1/installation/python.mdx";
import InstallationJavaScript from "/snippets/zh/v1/installation/js.mdx";
import InstallationGo from "/snippets/zh/v1/installation/go.mdx";
import InstallationRust from "/snippets/zh/v1/installation/rust.mdx";
import MapPython from "/snippets/zh/v1/map/base/python.mdx";
import MapJavaScript from "/snippets/zh/v1/map/base/js.mdx";
import MapGo from "/snippets/zh/v1/map/base/go.mdx";
import MapRust from "/snippets/zh/v1/map/base/rust.mdx";
import MapCURL from "/snippets/zh/v1/map/base/curl.mdx";
import MapResponse from "/snippets/zh/v1/map/base/output.mdx";

<div id="introducing-map">
  ## 介绍 /map
</div>

从单个 URL 快速生成整站链接地图的最简方式。适用于以下场景：

* 需要提示终端用户选择要抓取的链接
* 需要快速了解某网站包含的链接
* 需要抓取与特定主题相关的页面 (使用 `search` 参数) 
* 只需抓取网站的特定页面

<div id="mapping">
  ## 映射
</div>

<div id="map-endpoint">
  ### /map 端点
</div>

用于映射一个 URL 并获取该网站的 URL。它会返回网站上大多数链接。

<div id="installation">
  ### 安装
</div>

<CodeGroup>
  <InstallationPython />

  <InstallationJavaScript />

  <InstallationGo />

  <InstallationRust />
</CodeGroup>

<div id="usage">
  ### 使用
</div>

<CodeGroup>
  <MapPython />

  <MapJavaScript />

  <MapGo />

  <MapRust />

  <MapCURL />
</CodeGroup>

<div id="response">
  ### 响应
</div>

SDK 将直接返回数据对象。cURL 会按下方所示，原样返回载荷。

<MapResponse />

<div id="map-with-search">
  #### 带搜索功能的 Map
</div>

使用 `search` 参数的 Map 可在站点内搜索特定 URL。

```bash cURL
curl -X POST https://api.firecrawl.dev/v1/map \
    -H 'Content-Type: application/json' \
    -H 'Authorization: Bearer YOUR_API_KEY' \
    -d '{
      "url": "https://firecrawl.dev",
      "search": "docs"
    }'
```

响应将按相关性从高到低返回一个有序列表。

```json
{
  "status": "success",
  "links": [
    "https://docs.firecrawl.dev",
    "https://docs.firecrawl.dev/sdks/python",
    "https://docs.firecrawl.dev/learn/rag-llama3",
  ]
}
```

<div id="considerations">
  ## 注意事项
</div>

该端点以速度为优先，因此可能无法捕获站点上的所有链接。它主要依赖网站的 sitemap，并辅以缓存的抓取数据和搜索引擎结果。若需要更全面且最新的 URL 列表，建议改用 [/crawl](/zh/features/crawl) 端点。