```js Node
import { Firecrawl } from 'firecrawl';

const firecrawl = new Firecrawl({
  // 開始にAPIキーは不要です。より高いRate Limitsを利用するには追加してください:
  // apiKey: "fc-YOUR-API-KEY",
});

const result = await firecrawl.scrape('https://joesdiner.com/menu', {
    formats: ['menu']
});

console.log(result.menu);
```