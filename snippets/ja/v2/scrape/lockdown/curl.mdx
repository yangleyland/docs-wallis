```bash cURL
# 開始にAPI keyは不要です — より高いrate limitsを利用するには -H "Authorization: Bearer fc-YOUR_API_KEY" を追加してください:
curl -X POST https://api.firecrawl.dev/v2/scrape \
  -H 'Content-Type: application/json' \
  -d '{
    "url": "https://firecrawl.dev",
    "formats": ["markdown"],
    "lockdown": true
  }'
```