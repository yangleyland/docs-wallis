```rust Rust
let crawl_status = app.check_crawl_status(crawl_id).await;

match crawl_status {
    Ok(data) => println!("クロールステータス:\n{:#?}", data),
    Err(e) => eprintln!("クロールステータスの確認に失敗しました: {}", e),
}
```
