```js Node
const crawlResponse = await app.checkCrawlStatus("<crawl_id>");

if (!crawlResponse.success) {
  throw new Error(`クロールのステータスの確認に失敗しました: ${crawlResponse.error}`)
}

console.log(crawlResponse)
```
