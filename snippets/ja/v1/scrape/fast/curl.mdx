```bash cURL
# 開始するためにAPIキーは不要です — より高いRate Limitsを利用するには -H "Authorization: Bearer fc-YOUR_API_KEY" を追加してください:
curl -X POST https://api.firecrawl.dev/v1/scrape \
  -H 'Content-Type: application/json' \
  -d '{
    "url": "https://firecrawl.dev",
    "formats": ["markdown"],
    "maxAge": 3600000
  }'
```