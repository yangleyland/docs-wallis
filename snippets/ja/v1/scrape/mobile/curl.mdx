```bash cURL
# 開始にAPI keyは不要です — より高いrate limitsを利用するには -H "Authorization: Bearer YOUR_API_KEY" を追加してください:
curl -X POST https://api.firecrawl.dev/v1/scrape \
    -H 'Content-Type: application/json' \
    -d '{
        "url": "google.com",
        "formats": ["markdown"],
        "mobile": true
    }'
```