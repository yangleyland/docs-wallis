```python Python
from firecrawl import FirecrawlApp

# クライアントを初期化する
firecrawl = FirecrawlApp(api_key="your_api_key")


# リアルタイム更新で調査を開始
def on_activity(activity):
    print(f"[{activity['type']}] {activity['message']}")

# 詳細調査を実行
results = firecrawl.deep_research(
    query="量子コンピューティングの最新動向は？",
    max_depth=5,
    time_limit=180,
    max_urls=15,
    on_activity=on_activity
)

# 調査結果にアクセス
print(f"最終分析: {results['data']['finalAnalysis']}")

print(f"出典: 参照{len(results['data']['sources'])}件")


```
