```js Node
import { Firecrawl } from 'firecrawl';

const firecrawl = new Firecrawl({ apiKey: "fc-YOUR-API-KEY" });

// 开始一次爬取
const { id } = await firecrawl.startCrawl('https://docs.firecrawl.dev', { limit: 5 });

// 获取爬取状态
const status = await firecrawl.getCrawlStatus(id);
console.log(status);
```