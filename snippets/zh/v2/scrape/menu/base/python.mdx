```python Python
from firecrawl import Firecrawl

firecrawl = Firecrawl(
  # 无需 API 密钥即可开始——添加一个以提高限流额度：
  # api_key='fc-YOUR_API_KEY',
)

result = firecrawl.scrape(
    url='https://joesdiner.com/menu',
    formats=['menu']
)

print(result['menu'])
```