```python Python
from firecrawl import Firecrawl

firecrawl = Firecrawl(
  # 无需 API 密钥即可开始——添加后可提升限流额度：
  # api_key='fc-YOUR_API_KEY',
)

result = firecrawl.scrape(
    url='https://example.com/restaurants/the-bistro/menu',
    formats=['markdown', 'menu']
)

print(result['markdown'])
print(result['menu'])
```