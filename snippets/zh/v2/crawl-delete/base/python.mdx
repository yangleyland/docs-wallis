```python Python
from firecrawl import Firecrawl

firecrawl = Firecrawl(api_key="fc-你的 API Key")
resp = firecrawl.start_crawl(url="https://docs.firecrawl.dev", limit=10)
ok = firecrawl.cancel_crawl(resp.id)
print("已取消:", ok)
```
