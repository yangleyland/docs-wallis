```python Python
from firecrawl import FirecrawlApp

# 使用你的 API 密钥初始化 FirecrawlApp
app = FirecrawlApp(api_key='your_api_key')

data = app.extract([
  'https://docs.firecrawl.dev/',
  'https://firecrawl.dev/'
], prompt="从页面提取 Firecrawl 的使命。")
print(data)
```
