```js Node
import { z } from "zod";

// 定义用于提取内容的架构
const schema = z.object({
  company_mission: z.string(),
});

const scrapeResult = await app.extract([], {
  prompt: "从 Firecrawl 网站提取公司使命。",
  schema: schema
});

if (!scrapeResult.success) {
  throw new Error(`抓取失败：${scrapeResult.error}`)
}

console.log(scrapeResult.data);
```
