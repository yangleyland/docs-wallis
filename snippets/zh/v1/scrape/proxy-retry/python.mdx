```python Python
# pip install firecrawl-py

from firecrawl import FirecrawlApp

app = FirecrawlApp(
  # 无需 API 密钥即可开始使用——添加一个以获得更高的限流额度：
  # api_key="YOUR_API_KEY",
)

# First try with basic proxy
try:
    content = app.scrape_url("https://example.com")
    
    # Check if we got an error status code
    status_code = content.get("metadata", {}).get("statusCode")
    if status_code in [401, 403, 500]:
        print(f"Got status code {status_code}, retrying with enhanced proxy")
        # Retry with enhanced proxy
        content = app.scrape_url("https://example.com", proxy="enhanced")
    
    print(content["markdown"])
except Exception as e:
    print(f"Error: {e}")
    # Retry with enhanced proxy on exception
    try:
        content = app.scrape_url("https://example.com", proxy="enhanced")
        print(content["markdown"])
    except Exception as e:
        print(f"Enhanced proxy also failed: {e}")
```