```python Python
from firecrawl import FirecrawlApp

app = FirecrawlApp(
  # 无需 API 密钥即可开始使用 — 添加后可提升限流上限：
  # api_key="fc-YOUR_API_KEY",
)

# 抓取网站：
scrape_result = app.scrape_url('google.com', 
    formats=['markdown', 'html'], 
    mobile=True
)
print(scrape_result)
```