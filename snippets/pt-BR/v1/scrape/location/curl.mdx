```bash cURL
# Nenhuma chave de API é necessária para começar — adicione -H "Authorization: Bearer YOUR_API_KEY" para limites de taxa mais altos:
curl -X POST https://api.firecrawl.dev/v1/scrape \
    -H 'Content-Type: application/json' \
    -d '{
        "url": "airbnb.com",
        "formats": ["markdown"],
        "location": {
            "country": "BR",
            "languages": ["pt-BR"]
        }
    }'
```