```rust Rust
let crawl_status = app.check_crawl_status(crawl_id).await;

match crawl_status {
    Ok(data) => println!("Status do crawl:\n{:#?}", data),
    Err(e) => eprintln!("Falha ao verificar o status do crawl: {}", e),
}
```
