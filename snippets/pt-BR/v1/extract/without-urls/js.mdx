```js Node
import { z } from "zod";

// Defina o esquema para extrair o conteúdo
const schema = z.object({
  company_mission: z.string(),
});

const scrapeResult = await app.extract([], {
  prompt: "Extraia a missão da empresa no site da Firecrawl.",
  schema: schema
});

if (!scrapeResult.success) {
  throw new Error(`Falha ao extrair: ${scrapeResult.error}`)
}

console.log(scrapeResult.data);
```
