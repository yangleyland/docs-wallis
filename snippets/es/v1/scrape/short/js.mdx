```js Node
// Extraer un sitio web:
const scrapeResult = await app.scrapeUrl('firecrawl.dev', { formats: ['markdown', 'html'] });

if (!scrapeResult.success) {
  throw new Error(`No se pudo extraer: ${scrapeResult.error}`)
}

console.log(scrapeResult)
```
