```bash cURL
# No se necesita API key para comenzar — agrega -H "Authorization: Bearer YOUR_API_KEY" para límites de tasa más altos:
curl -X POST https://api.firecrawl.dev/v1/scrape \
    -H 'Content-Type: application/json' \
    -d '{
      "url": "https://example.com",
      "proxy": "enhanced"
    }'
```