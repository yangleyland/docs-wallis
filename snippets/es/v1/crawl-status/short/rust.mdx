```rust Rust
let crawl_status = app.check_crawl_status(crawl_id).await;

match crawl_status {
    Ok(data) => println!("Estado del rastreo:\n{:#?}", data),
    Err(e) => eprintln!("Error al comprobar el estado del rastreo: {}", e),
}
```
