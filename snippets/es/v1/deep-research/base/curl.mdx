```bash cURL
curl -X POST "https://api.firecrawl.dev/v1/deep-research" \
  -H "Authorization: Bearer tu_api_key" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "¿Cuáles son los últimos avances en computación cuántica?",
    "maxDepth": 5,
    "timeLimit": 180,
    "maxUrls": 15
  }'
```
