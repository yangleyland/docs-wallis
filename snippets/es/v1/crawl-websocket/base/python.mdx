```python Python
# dentro de una función asíncrona...
nest_asyncio.apply()

# Definir controladores de eventos
def on_document(detail):
    print("DOC", detail)

def on_error(detail):
    print("ERR", detail['error'])

def on_done(detail):
    print("DONE", detail['status'])

    # Función para iniciar el rastreo y la observación
async def start_crawl_and_watch():
    # Iniciar el trabajo de rastreo y obtener el watcher
    watcher = app.crawl_url_and_watch('firecrawl.dev', limit=5)

    # Agregar listeners de eventos
    watcher.add_event_listener("document", on_document)
    watcher.add_event_listener("error", on_error)
    watcher.add_event_listener("done", on_done)

    # Iniciar el watcher
    await watcher.connect()

# Ejecutar el bucle de eventos
await start_crawl_and_watch()
```
