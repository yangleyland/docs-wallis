```bash cURL
curl -X POST "https://api.firecrawl.dev/v2/map" \
  -H "Autorización: Bearer $FIRECRAWL_API_KEY" \
  -H "Tipo-Contenido: application/json" \
  -d '{
    "url": "https://example.com",
    "location": { "country": "US", "languages": ["en"] }
  }'
```
