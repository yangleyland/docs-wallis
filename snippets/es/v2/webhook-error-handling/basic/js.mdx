```javascript
app.post('/webhook', async (req, res) => {
  try {
    await processWebhook(req.body);
    res.status(200).send('OK');
  } catch (error) {
    console.error('Error al procesar el webhook:', error);
    // Still acknowledge receipt to prevent retries
    res.status(200).send('OK');
  }
});

async function processWebhook(payload) {
  const { success, type, id, data } = payload;
  
  // Tu lógica de procesamiento del webhook aquí
  if (type.includes('.page') && data.length > 0) {
    // Procesar los datos de la página obtenida
    await saveToDatabase(data[0]);
  }
}

async function saveToDatabase(pageData) {
  // Ejemplo: guardar en la base de datos
  console.log('Guardando en la base de datos:', pageData.metadata.sourceURL);
}
```
