```python Python
from firecrawl import Firecrawl

firecrawl = Firecrawl(
  # No se necesita API key para empezar — añade una para obtener límites de tasa más altos:
  # api_key='fc-YOUR_API_KEY',
)

result = firecrawl.scrape(
    url='https://joesdiner.com/menu',
    formats=['menu']
)

print(result['menu'])
```