```python Python
import asyncio
from firecrawl import AsyncFirecrawl

async def main():
    firecrawl = AsyncFirecrawl(api_key="fc-TU-CLAVE-DE-API")
    job = await firecrawl.crawl("https://docs.firecrawl.dev", limit=5, poll_interval=1, timeout=120)
    print(job.status)

asyncio.run(main())
```
