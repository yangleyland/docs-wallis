```js Node
import { Firecrawl, CrawlParams, CrawlStatusResponse } from 'firecrawl';

const app = new Firecrawl({apiKey: "fc-YOUR_API_KEY"});

// Extraire un site web
const scrapeResponse = await app.scrapeUrl('https://firecrawl.dev', {
  formats: ['markdown', 'html'],
});

if (!scrapeResponse.success) {
  throw new Error(`Échec de l'extraction : ${scrapeResponse.error}`)
}

console.log(scrapeResponse)

// Explorer un site web
const crawlResponse = await app.crawlUrl('https://firecrawl.dev', {
  limit: 100,
  scrapeOptions: {
    formats: ['markdown', 'html'],
  }
});

if (!crawlResponse.success) {
  throw new Error(`Échec de l'exploration : ${crawlResponse.error}`)
}

console.log(crawlResponse)
```