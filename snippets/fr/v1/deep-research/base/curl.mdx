```bash cURL
curl -X POST "https://api.firecrawl.dev/v1/deep-research" \
  -H "Authorization: Bearer votre_clé_api" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "Quelles sont les dernières avancées de l’informatique quantique ?",
    "maxDepth": 5,
    "timeLimit": 180,
    "maxUrls": 15
  }'
```
