```javascript JavaScript
import { Firecrawl } from 'firecrawl';

const app = new Firecrawl({
  // Aucune clé API requise pour démarrer — ajoutez-en une pour des limites de débit plus élevées :
  // apiKey: "fc-YOUR_API_KEY",
});

// Utilisez les données en cache si elles datent de moins d'une heure (3600000 ms)
// Cela peut être 5× plus rapide qu'un nouveau scrape !
const scrapeResult = await app.scrapeUrl('https://firecrawl.dev', {
  formats: ['markdown'],
  maxAge: 3600000 // 1 heure en millisecondes
});

console.log(scrapeResult.markdown);
```