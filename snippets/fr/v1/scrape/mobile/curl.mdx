```bash cURL
# Aucune clé API nécessaire pour démarrer — ajoutez -H "Authorization: Bearer VOTRE_CLÉ_API" pour des limites de débit plus élevées :
curl -X POST https://api.firecrawl.dev/v1/scrape \
    -H 'Content-Type: application/json' \
    -d '{
        "url": "google.com",
        "formats": ["markdown"],
        "mobile": true
    }'
```