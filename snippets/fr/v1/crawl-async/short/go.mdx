```go Go
crawlStatus, err := app.AsyncCrawlUrl("https://firecrawl.dev", nil, nil)
if err != nil {
  log.Fatalf("Échec de l’envoi de la requête d’exploration : %v", err)
}
fmt.Println(crawlStatus) 
```
