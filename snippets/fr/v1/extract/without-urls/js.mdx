```js Node.js
import { z } from "zod";

// Définir le schéma pour l’extraction du contenu
const schema = z.object({
  company_mission: z.string(),
});

const scrapeResult = await app.extract([], {
  prompt: "Extrait la mission de l’entreprise depuis le site de Firecrawl.",
  schema: schema
});

if (!scrapeResult.success) {
  throw new Error(`Échec de l’extraction : ${scrapeResult.error}`)
}

console.log(scrapeResult.data);
```
