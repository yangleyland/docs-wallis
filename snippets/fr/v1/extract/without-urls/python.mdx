```python Python
from pydantic import BaseModel

class ExtractSchema(BaseModel):
    company_mission: str


# Définir le prompt pour l’extraction
prompt = 'Extrait la mission de l’entreprise depuis le site web de Firecrawl.'

# Lancer l’extraction
scrape_result = app.extract(prompt=prompt, schema=ExtractSchema.model_json_schema())

# Vérifier si l’extraction a abouti
if not scrape_result.success:
    raise Exception(f"Échec de l’extraction : {scrape_result.error}")

# Afficher les données extraites
print(scrape_result.data)
```
