```python Python
from firecrawl import FirecrawlApp

# Initialisez FirecrawlApp avec votre clé d’API
app = FirecrawlApp(api_key='your_api_key')

data = app.extract([
  'https://docs.firecrawl.dev/',
  'https://firecrawl.dev/'
], prompt="Extrait la mission de Firecrawl à partir de la page.")
print(data)
```
