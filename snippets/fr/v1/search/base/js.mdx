```js Node
import { Firecrawl, SearchResponse } from 'firecrawl';

// Initialisez le client avec votre clé d'API
const app = new Firecrawl({
  // Aucune clé d'API requise pour démarrer — ajoutez-en une pour des limites de débit plus élevées :
  // apiKey: "fc-YOUR_API_KEY",
});

// Effectuez une recherche basique
app.search("firecrawl web scraping", { limit: 5 })
  .then(searchResult => {
    // Traitez les résultats de la recherche
    searchResult.data.forEach(result => {
      console.log(`Title: ${result.title}`);
      console.log(`URL: ${result.url}`);
      console.log(`Description: ${result.description}`);
    });
  }); 
```