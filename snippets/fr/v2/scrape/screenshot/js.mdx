```js Node
import { Firecrawl } from 'firecrawl';

const firecrawl = new Firecrawl({
  // Aucune clé API requise pour démarrer — ajoutez-en une pour des limites de débit plus élevées :
  // apiKey: "fc-VOTRE-CLÉ-API",
});

const doc = await firecrawl.scrape('https://example.com', {
  formats: [{ type: 'screenshot', fullPage: true, quality: 80, viewport: { width: 1280, height: 800 } }]
});

console.log(doc.screenshot);
```